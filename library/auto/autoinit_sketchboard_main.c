/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for sketchboard
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <intuition/intuition.h>
#include <intuition/classes.h>

#include <interfaces/sketchboard.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct SketchBoardIFace * ISketchBoard = NULL;
static struct Library * __SketchBoardBase = NULL;
static struct SketchBoardIFace * __ISketchBoard = NULL;

/****************************************************************************/

extern struct Library * SketchBoardBase;

/****************************************************************************/

void sketchboard_main_constructor(void)
{
    if (ISketchBoard != NULL)
    {
        return; /* Someone was quicker */
    }
    if (SketchBoardBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __SketchBoardBase = SketchBoardBase = IExec->OpenLibrary("gadgets/sketchboard.gadget", 0);
        if (SketchBoardBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "gadgets/sketchboard.gadget");
            abort();
        }
    }

    __ISketchBoard = ISketchBoard = (struct SketchBoardIFace *)IExec->GetInterface((struct Library *)SketchBoardBase, "main", 1, NULL);
    if (ISketchBoard == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "gadgets/sketchboard.gadget");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*sketchboard_main_constructor_ptr)(void) USED = sketchboard_main_constructor;

/****************************************************************************/

void sketchboard_main_destructor(void)
{
    if (__ISketchBoard)
    {
        IExec->DropInterface ((struct Interface *)__ISketchBoard);
    }
    if (__SketchBoardBase)
    {
        IExec->CloseLibrary(__SketchBoardBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*sketchboard_main_destructor_ptr)(void) USED = sketchboard_main_destructor;

/****************************************************************************/

