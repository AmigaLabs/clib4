/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for getscreenmode
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <intuition/intuition.h>
#include <intuition/classes.h>

#include <interfaces/getscreenmode.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct GetScreenModeIFace * IGetScreenMode = NULL;
static struct Library * __GetScreenModeBase = NULL;
static struct GetScreenModeIFace * __IGetScreenMode = NULL;

/****************************************************************************/

extern struct Library * GetScreenModeBase;

/****************************************************************************/

void getscreenmode_main_constructor(void)
{
    if (IGetScreenMode != NULL)
    {
        return; /* Someone was quicker */
    }
    if (GetScreenModeBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __GetScreenModeBase = GetScreenModeBase = IExec->OpenLibrary("gadgets/getscreenmode.gadget", 0);
        if (GetScreenModeBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "gadgets/getscreenmode.gadget");
            abort();
        }
    }

    __IGetScreenMode = IGetScreenMode = (struct GetScreenModeIFace *)IExec->GetInterface((struct Library *)GetScreenModeBase, "main", 1, NULL);
    if (IGetScreenMode == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "gadgets/getscreenmode.gadget");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*getscreenmode_main_constructor_ptr)(void) USED = getscreenmode_main_constructor;

/****************************************************************************/

void getscreenmode_main_destructor(void)
{
    if (__IGetScreenMode)
    {
        IExec->DropInterface ((struct Interface *)__IGetScreenMode);
    }
    if (__GetScreenModeBase)
    {
        IExec->CloseLibrary(__GetScreenModeBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*getscreenmode_main_destructor_ptr)(void) USED = getscreenmode_main_destructor;

/****************************************************************************/

