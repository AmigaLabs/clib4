/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for palette
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <intuition/intuition.h>
#include <intuition/classes.h>

#include <interfaces/palette.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct PaletteIFace * IPalette = NULL;
static struct Library * __PaletteBase = NULL;
static struct PaletteIFace * __IPalette = NULL;

/****************************************************************************/

extern struct Library * PaletteBase;

/****************************************************************************/

void palette_main_constructor(void)
{
    if (IPalette != NULL)
    {
        return; /* Someone was quicker */
    }
    if (PaletteBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __PaletteBase = PaletteBase = IExec->OpenLibrary("gadgets/palette.gadget", 0);
        if (PaletteBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "gadgets/palette.gadget");
            abort();
        }
    }

    __IPalette = IPalette = (struct PaletteIFace *)IExec->GetInterface((struct Library *)PaletteBase, "main", 1, NULL);
    if (IPalette == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "gadgets/palette.gadget");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*palette_main_constructor_ptr)(void) USED = palette_main_constructor;

/****************************************************************************/

void palette_main_destructor(void)
{
    if (__IPalette)
    {
        IExec->DropInterface ((struct Interface *)__IPalette);
    }
    if (__PaletteBase)
    {
        IExec->CloseLibrary(__PaletteBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*palette_main_destructor_ptr)(void) USED = palette_main_destructor;

/****************************************************************************/

