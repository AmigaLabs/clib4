/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for z
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <libraries/z.h>

#include <interfaces/z.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct ZIFace * IZ = NULL;
static struct Library * __ZBase = NULL;
static struct ZIFace * __IZ = NULL;

/****************************************************************************/

extern struct Library * ZBase;

/****************************************************************************/

void z_main_constructor(void)
{
    if (IZ != NULL)
    {
        return; /* Someone was quicker */
    }
    if (ZBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __ZBase = ZBase = IExec->OpenLibrary("z.library", 0);
        if (ZBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "z.library");
            abort();
        }
    }

    __IZ = IZ = (struct ZIFace *)IExec->GetInterface((struct Library *)ZBase, "main", 1, NULL);
    if (IZ == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "z.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*z_main_constructor_ptr)(void) USED = z_main_constructor;

/****************************************************************************/

void z_main_destructor(void)
{
    if (__IZ)
    {
        IExec->DropInterface ((struct Interface *)__IZ);
    }
    if (__ZBase)
    {
        IExec->CloseLibrary(__ZBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*z_main_destructor_ptr)(void) USED = z_main_destructor;

/****************************************************************************/

