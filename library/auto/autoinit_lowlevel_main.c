/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for lowlevel
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/types.h>
#include <exec/interrupts.h>
#include <utility/tagitem.h>
#include <devices/timer.h>
#include <libraries/lowlevel.h>

#include <interfaces/lowlevel.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct LowLevelIFace * ILowLevel = NULL;
static struct Library * __LowLevelBase = NULL;
static struct LowLevelIFace * __ILowLevel = NULL;

/****************************************************************************/

extern struct Library * LowLevelBase;

/****************************************************************************/

void lowlevel_main_constructor(void)
{
    if (ILowLevel != NULL)
    {
        return; /* Someone was quicker */
    }
    if (LowLevelBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __LowLevelBase = LowLevelBase = IExec->OpenLibrary("lowlevel.library", 0);
        if (LowLevelBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "lowlevel.library");
            abort();
        }
    }

    __ILowLevel = ILowLevel = (struct LowLevelIFace *)IExec->GetInterface((struct Library *)LowLevelBase, "main", 1, NULL);
    if (ILowLevel == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "lowlevel.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*lowlevel_main_constructor_ptr)(void) USED = lowlevel_main_constructor;

/****************************************************************************/

void lowlevel_main_destructor(void)
{
    if (__ILowLevel)
    {
        IExec->DropInterface ((struct Interface *)__ILowLevel);
    }
    if (__LowLevelBase)
    {
        IExec->CloseLibrary(__LowLevelBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*lowlevel_main_destructor_ptr)(void) USED = lowlevel_main_destructor;

/****************************************************************************/

