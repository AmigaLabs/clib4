/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for hunk
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <utility/tagitem.h>
#include <libraries/hunk.h>
#include <dos/dos.h>
#include <interfaces/hunk.h>

#include <interfaces/hunk.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct HunkIFace * IHunk = NULL;
static struct Library * __HunkBase = NULL;
static struct HunkIFace * __IHunk = NULL;

/****************************************************************************/

extern struct Library * HunkBase;

/****************************************************************************/

void hunk_main_constructor(void)
{
    if (IHunk != NULL)
    {
        return; /* Someone was quicker */
    }
    if (HunkBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __HunkBase = HunkBase = IExec->OpenLibrary("hunk.library", 0);
        if (HunkBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "hunk.library");
            abort();
        }
    }

    __IHunk = IHunk = (struct HunkIFace *)IExec->GetInterface((struct Library *)HunkBase, "main", 1, NULL);
    if (IHunk == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "hunk.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*hunk_main_constructor_ptr)(void) USED = hunk_main_constructor;

/****************************************************************************/

void hunk_main_destructor(void)
{
    if (__IHunk)
    {
        IExec->DropInterface ((struct Interface *)__IHunk);
    }
    if (__HunkBase)
    {
        IExec->CloseLibrary(__HunkBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*hunk_main_destructor_ptr)(void) USED = hunk_main_destructor;

/****************************************************************************/

