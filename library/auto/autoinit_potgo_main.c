/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for potgo
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/types.h>

#include <interfaces/potgo.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct PotgoIFace * IPotgo = NULL;
static struct Library * __PotgoBase = NULL;
static struct PotgoIFace * __IPotgo = NULL;

/****************************************************************************/

extern struct Library * PotgoBase;

/****************************************************************************/

void potgo_main_constructor(void)
{
    if (IPotgo != NULL)
    {
        return; /* Someone was quicker */
    }
    if (PotgoBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __PotgoBase = PotgoBase = IExec->OpenLibrary("potgo.library", 0);
        if (PotgoBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "potgo.library");
            abort();
        }
    }

    __IPotgo = IPotgo = (struct PotgoIFace *)IExec->GetInterface((struct Library *)PotgoBase, "main", 1, NULL);
    if (IPotgo == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "potgo.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*potgo_main_constructor_ptr)(void) USED = potgo_main_constructor;

/****************************************************************************/

void potgo_main_destructor(void)
{
    if (__IPotgo)
    {
        IExec->DropInterface ((struct Interface *)__IPotgo);
    }
    if (__PotgoBase)
    {
        IExec->CloseLibrary(__PotgoBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*potgo_main_destructor_ptr)(void) USED = potgo_main_destructor;

/****************************************************************************/

