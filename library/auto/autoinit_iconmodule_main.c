/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for iconmodule
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <workbench/workbench.h>
#include <workbench/icon.h>
#include <workbench/iconmodule.h>

#include <interfaces/iconmodule.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct IconModuleIFace * IIconModule = NULL;
static struct Library * __IconModuleBase = NULL;
static struct IconModuleIFace * __IIconModule = NULL;

/****************************************************************************/

extern struct Library * IconModuleBase;

/****************************************************************************/

void iconmodule_main_constructor(void)
{
    if (IIconModule != NULL)
    {
        return; /* Someone was quicker */
    }
    if (IconModuleBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __IconModuleBase = IconModuleBase = IExec->OpenLibrary("iconmodules/generic.iconmodule", 0);
        if (IconModuleBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "iconmodules/generic.iconmodule");
            abort();
        }
    }

    __IIconModule = IIconModule = (struct IconModuleIFace *)IExec->GetInterface((struct Library *)IconModuleBase, "main", 1, NULL);
    if (IIconModule == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "iconmodules/generic.iconmodule");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*iconmodule_main_constructor_ptr)(void) USED = iconmodule_main_constructor;

/****************************************************************************/

void iconmodule_main_destructor(void)
{
    if (__IIconModule)
    {
        IExec->DropInterface ((struct Interface *)__IIconModule);
    }
    if (__IconModuleBase)
    {
        IExec->CloseLibrary(__IconModuleBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*iconmodule_main_destructor_ptr)(void) USED = iconmodule_main_destructor;

/****************************************************************************/

