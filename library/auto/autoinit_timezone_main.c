/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for timezone
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <libraries/timezone.h>

#include <interfaces/timezone.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct TimezoneIFace * ITimezone = NULL;
static struct Library * __TimezoneBase = NULL;
static struct TimezoneIFace * __ITimezone = NULL;

/****************************************************************************/

extern struct Library * TimezoneBase;

/****************************************************************************/

void timezone_main_constructor(void)
{
    if (ITimezone != NULL)
    {
        return; /* Someone was quicker */
    }
    if (TimezoneBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __TimezoneBase = TimezoneBase = IExec->OpenLibrary("timezone.library", 0);
        if (TimezoneBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "timezone.library");
            abort();
        }
    }

    __ITimezone = ITimezone = (struct TimezoneIFace *)IExec->GetInterface((struct Library *)TimezoneBase, "main", 1, NULL);
    if (ITimezone == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "timezone.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*timezone_main_constructor_ptr)(void) USED = timezone_main_constructor;

/****************************************************************************/

void timezone_main_destructor(void)
{
    if (__ITimezone)
    {
        IExec->DropInterface ((struct Interface *)__ITimezone);
    }
    if (__TimezoneBase)
    {
        IExec->CloseLibrary(__TimezoneBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*timezone_main_destructor_ptr)(void) USED = timezone_main_destructor;

/****************************************************************************/

