/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for misc
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/types.h>

#include <interfaces/misc.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct MiscIFace * IMisc = NULL;
static struct Library * __MiscBase = NULL;
static struct MiscIFace * __IMisc = NULL;

/****************************************************************************/

extern struct Library * MiscBase;

/****************************************************************************/

void misc_main_constructor(void)
{
    if (IMisc != NULL)
    {
        return; /* Someone was quicker */
    }
    if (MiscBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __MiscBase = MiscBase = IExec->OpenLibrary("misc.resource", 0);
        if (MiscBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "misc.resource");
            abort();
        }
    }

    __IMisc = IMisc = (struct MiscIFace *)IExec->GetInterface((struct Library *)MiscBase, "main", 1, NULL);
    if (IMisc == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "misc.resource");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*misc_main_constructor_ptr)(void) USED = misc_main_constructor;

/****************************************************************************/

void misc_main_destructor(void)
{
    if (__IMisc)
    {
        IExec->DropInterface ((struct Interface *)__IMisc);
    }
    if (__MiscBase)
    {
        IExec->CloseLibrary(__MiscBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*misc_main_destructor_ptr)(void) USED = misc_main_destructor;

/****************************************************************************/

