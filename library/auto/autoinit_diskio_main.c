/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for diskio
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/types.h>
#include <utility/tagitem.h>
#include <libraries/diskio.h>

#include <interfaces/diskio.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct DiskIOIFace * IDiskIO = NULL;
static struct Library * __DiskIOBase = NULL;
static struct DiskIOIFace * __IDiskIO = NULL;

/****************************************************************************/

extern struct Library * DiskIOBase;

/****************************************************************************/

void diskio_main_constructor(void)
{
    if (IDiskIO != NULL)
    {
        return; /* Someone was quicker */
    }
    if (DiskIOBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __DiskIOBase = DiskIOBase = IExec->OpenLibrary("diskio.library", 0);
        if (DiskIOBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "diskio.library");
            abort();
        }
    }

    __IDiskIO = IDiskIO = (struct DiskIOIFace *)IExec->GetInterface((struct Library *)DiskIOBase, "main", 1, NULL);
    if (IDiskIO == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "diskio.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*diskio_main_constructor_ptr)(void) USED = diskio_main_constructor;

/****************************************************************************/

void diskio_main_destructor(void)
{
    if (__IDiskIO)
    {
        IExec->DropInterface ((struct Interface *)__IDiskIO);
    }
    if (__DiskIOBase)
    {
        IExec->CloseLibrary(__DiskIOBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*diskio_main_destructor_ptr)(void) USED = diskio_main_destructor;

/****************************************************************************/

