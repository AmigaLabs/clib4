/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for thumblist
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <gadgets/thumblist.h>
#include <intuition/classes.h>

#include <interfaces/thumblist.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct ThumbListIFace * IThumbList = NULL;
static struct Library * __ThumbListBase = NULL;
static struct ThumbListIFace * __IThumbList = NULL;

/****************************************************************************/

extern struct Library * ThumbListBase;

/****************************************************************************/

void thumblist_main_constructor(void)
{
    if (IThumbList != NULL)
    {
        return; /* Someone was quicker */
    }
    if (ThumbListBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __ThumbListBase = ThumbListBase = IExec->OpenLibrary("thumblist.gadget", 0);
        if (ThumbListBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "thumblist.gadget");
            abort();
        }
    }

    __IThumbList = IThumbList = (struct ThumbListIFace *)IExec->GetInterface((struct Library *)ThumbListBase, "main", 1, NULL);
    if (IThumbList == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "thumblist.gadget");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*thumblist_main_constructor_ptr)(void) USED = thumblist_main_constructor;

/****************************************************************************/

void thumblist_main_destructor(void)
{
    if (__IThumbList)
    {
        IExec->DropInterface ((struct Interface *)__IThumbList);
    }
    if (__ThumbListBase)
    {
        IExec->CloseLibrary(__ThumbListBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*thumblist_main_destructor_ptr)(void) USED = thumblist_main_destructor;

/****************************************************************************/

