/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for getcolor
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <intuition/intuition.h>
#include <intuition/classes.h>

#include <interfaces/getcolor.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <proto/intuition.h>
#include <stdlib.h>

static struct Library * __IntuitionBase = NULL;
static struct IntuitionIFace *__IIntuition = NULL;
struct GetColorIFace * IGetColor = NULL;
static struct ClassLibrary * __GetColorBase = NULL;
static struct GetColorIFace * __IGetColor = NULL;

/****************************************************************************/

extern struct ClassLibrary * GetColorBase;
extern Class * GetColorClass;

/****************************************************************************/

void getcolor_main_constructor(void)
{
    if (IGetColor != NULL)
    {
        return; /* Someone was quicker */
    }
    if (GetColorBase == NULL) /* Library base is NULL, we need to open it */
    {
        if (IIntuition == NULL)
        {
            if (IntuitionBase == NULL)
            {
                __IntuitionBase = IntuitionBase = IExec->OpenLibrary("intuition.library", 0);
                if (IntuitionBase == NULL)
                {
                    BPTR StdErr = IDOS->ErrorOutput();
                    if (StdErr == ZERO)
                        StdErr = IDOS->Output();
                    IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "intuition.library");
                    abort();
                }
            }
            __IIntuition = IIntuition = (struct IntuitionIFace *)IExec->GetInterface(IntuitionBase, "main", 1, NULL);
            if (IIntuition == NULL)
            {
                BPTR StdErr = IDOS->ErrorOutput();
                if (StdErr == ZERO)
                    StdErr = IDOS->Output();
                IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "intuition.library");
                abort();
            }
        }
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __GetColorBase = GetColorBase = IIntuition->OpenClass("gadgets/getcolor.gadget", 0, &GetColorClass);
        if (GetColorBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "gadgets/getcolor.gadget");
            abort();
        }
    }

    __IGetColor = IGetColor = (struct GetColorIFace *)IExec->GetInterface((struct Library *)GetColorBase, "main", 1, NULL);
    if (IGetColor == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "gadgets/getcolor.gadget");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*getcolor_main_constructor_ptr)(void) USED = getcolor_main_constructor;

/****************************************************************************/

void getcolor_main_destructor(void)
{
    if (__IGetColor)
    {
        IExec->DropInterface ((struct Interface *)__IGetColor);
    }
    if (__GetColorBase != NULL)
    {
        IIntuition->CloseClass(__GetColorBase);
    }
    if (__IIntuition != NULL)
    {
        IExec->DropInterface((struct Interface *)__IIntuition);
    }
    if (__IntuitionBase != NULL)
    {
        IExec->CloseLibrary(__IntuitionBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*getcolor_main_destructor_ptr)(void) USED = getcolor_main_destructor;

/****************************************************************************/

