/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for table
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <images/table.h>
#include <intuition/classes.h>

#include <interfaces/table.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct TableIFace * ITable = NULL;
static struct Library * __TableBase = NULL;
static struct TableIFace * __ITable = NULL;

/****************************************************************************/

extern struct Library * TableBase;

/****************************************************************************/

void table_main_constructor(void)
{
    if (ITable != NULL)
    {
        return; /* Someone was quicker */
    }
    if (TableBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __TableBase = TableBase = IExec->OpenLibrary("table.image", 0);
        if (TableBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "table.image");
            abort();
        }
    }

    __ITable = ITable = (struct TableIFace *)IExec->GetInterface((struct Library *)TableBase, "main", 1, NULL);
    if (ITable == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "table.image");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*table_main_constructor_ptr)(void) USED = table_main_constructor;

/****************************************************************************/

void table_main_destructor(void)
{
    if (__ITable)
    {
        IExec->DropInterface ((struct Interface *)__ITable);
    }
    if (__TableBase)
    {
        IExec->CloseLibrary(__TableBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*table_main_destructor_ptr)(void) USED = table_main_destructor;

/****************************************************************************/

