/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for pasemi_dma
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/types.h>
#include <resources/pasemi_dma.h>

#include <interfaces/pasemi_dma.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct PASDMAIFace * IPASDMA = NULL;
static struct Library * __PASemiDMABase = NULL;
static struct PASDMAIFace * __IPASDMA = NULL;

/****************************************************************************/

extern struct Library * PASemiDMABase;

/****************************************************************************/

void pasemi_dma_main_constructor(void)
{
    if (IPASDMA != NULL)
    {
        return; /* Someone was quicker */
    }
    if (PASemiDMABase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __PASemiDMABase = PASemiDMABase = IExec->OpenLibrary("PASemi_DMA.resource", 0);
        if (PASemiDMABase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "PASemi_DMA.resource");
            abort();
        }
    }

    __IPASDMA = IPASDMA = (struct PASDMAIFace *)IExec->GetInterface((struct Library *)PASemiDMABase, "main", 1, NULL);
    if (IPASDMA == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "PASemi_DMA.resource");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*pasemi_dma_main_constructor_ptr)(void) USED = pasemi_dma_main_constructor;

/****************************************************************************/

void pasemi_dma_main_destructor(void)
{
    if (__IPASDMA)
    {
        IExec->DropInterface ((struct Interface *)__IPASDMA);
    }
    if (__PASemiDMABase)
    {
        IExec->CloseLibrary(__PASemiDMABase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*pasemi_dma_main_destructor_ptr)(void) USED = pasemi_dma_main_destructor;

/****************************************************************************/

