/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for locale
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/types.h>
#include <libraries/locale.h>
#include <dos/dos.h>
#include <utility/hooks.h>
#include <utility/tagitem.h>
#include <rexx/storage.h>

#include <interfaces/locale.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct LocaleIFace * ILocale = NULL;
static struct Library * __LocaleBase = NULL;
static struct LocaleIFace * __ILocale = NULL;

/****************************************************************************/

extern struct Library * LocaleBase;

/****************************************************************************/

void locale_main_constructor(void)
{
    if (ILocale != NULL)
    {
        return; /* Someone was quicker */
    }
    if (LocaleBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __LocaleBase = LocaleBase = IExec->OpenLibrary("locale.library", 0);
        if (LocaleBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "locale.library");
            abort();
        }
    }

    __ILocale = ILocale = (struct LocaleIFace *)IExec->GetInterface((struct Library *)LocaleBase, "main", 1, NULL);
    if (ILocale == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "locale.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*locale_main_constructor_ptr)(void) USED = locale_main_constructor;

/****************************************************************************/

void locale_main_destructor(void)
{
    if (__ILocale)
    {
        IExec->DropInterface ((struct Interface *)__ILocale);
    }
    if (__LocaleBase)
    {
        IExec->CloseLibrary(__LocaleBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*locale_main_destructor_ptr)(void) USED = locale_main_destructor;

/****************************************************************************/

