/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for blankermodule
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/libraries.h>

#include <interfaces/blankermodule.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct BlankerModuleIFace * IBlankerModule = NULL;
static struct Library * __BlankerModule = NULL;
static struct BlankerModuleIFace * __IBlankerModule = NULL;

/****************************************************************************/

extern struct Library * BlankerModule;

/****************************************************************************/

void blankermodule_blankermodule_constructor(void)
{
    if (IBlankerModule != NULL)
    {
        return; /* Someone was quicker */
    }
    if (BlankerModule == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __BlankerModule = BlankerModule = IExec->OpenLibrary("blankermodule.library", 0);
        if (BlankerModule == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "blankermodule.library");
            abort();
        }
    }

    __IBlankerModule = IBlankerModule = (struct BlankerModuleIFace *)IExec->GetInterface((struct Library *)BlankerModule, "blankermodule", 1, NULL);
    if (IBlankerModule == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "blankermodule", 1, "blankermodule.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*blankermodule_blankermodule_constructor_ptr)(void) USED = blankermodule_blankermodule_constructor;

/****************************************************************************/

void blankermodule_blankermodule_destructor(void)
{
    if (__IBlankerModule)
    {
        IExec->DropInterface ((struct Interface *)__IBlankerModule);
    }
    if (__BlankerModule)
    {
        IExec->CloseLibrary(__BlankerModule);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*blankermodule_blankermodule_destructor_ptr)(void) USED = blankermodule_blankermodule_destructor;

/****************************************************************************/

