/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for glyph
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <intuition/intuition.h>
#include <intuition/classes.h>

#include <interfaces/glyph.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct GlyphIFace * IGlyph = NULL;
static struct Library * __GlyphBase = NULL;
static struct GlyphIFace * __IGlyph = NULL;

/****************************************************************************/

extern struct Library * GlyphBase;

/****************************************************************************/

void glyph_main_constructor(void)
{
    if (IGlyph != NULL)
    {
        return; /* Someone was quicker */
    }
    if (GlyphBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __GlyphBase = GlyphBase = IExec->OpenLibrary("images/glyph.image", 0);
        if (GlyphBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "images/glyph.image");
            abort();
        }
    }

    __IGlyph = IGlyph = (struct GlyphIFace *)IExec->GetInterface((struct Library *)GlyphBase, "main", 1, NULL);
    if (IGlyph == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "images/glyph.image");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*glyph_main_constructor_ptr)(void) USED = glyph_main_constructor;

/****************************************************************************/

void glyph_main_destructor(void)
{
    if (__IGlyph)
    {
        IExec->DropInterface ((struct Interface *)__IGlyph);
    }
    if (__GlyphBase)
    {
        IExec->CloseLibrary(__GlyphBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*glyph_main_destructor_ptr)(void) USED = glyph_main_destructor;

/****************************************************************************/

