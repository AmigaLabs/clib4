/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for codesets
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <libraries/codesets.h>

#include <interfaces/codesets.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct CodesetsIFace * ICodesets = NULL;
static struct Library * __CodesetsBase = NULL;
static struct CodesetsIFace * __ICodesets = NULL;

/****************************************************************************/

extern struct Library * CodesetsBase;

/****************************************************************************/

void codesets_main_constructor(void)
{
    if (ICodesets != NULL)
    {
        return; /* Someone was quicker */
    }
    if (CodesetsBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __CodesetsBase = CodesetsBase = IExec->OpenLibrary("codesets.library", 0);
        if (CodesetsBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "codesets.library");
            abort();
        }
    }

    __ICodesets = ICodesets = (struct CodesetsIFace *)IExec->GetInterface((struct Library *)CodesetsBase, "main", 1, NULL);
    if (ICodesets == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "codesets.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*codesets_main_constructor_ptr)(void) USED = codesets_main_constructor;

/****************************************************************************/

void codesets_main_destructor(void)
{
    if (__ICodesets)
    {
        IExec->DropInterface ((struct Interface *)__ICodesets);
    }
    if (__CodesetsBase)
    {
        IExec->CloseLibrary(__CodesetsBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*codesets_main_destructor_ptr)(void) USED = codesets_main_destructor;

/****************************************************************************/

