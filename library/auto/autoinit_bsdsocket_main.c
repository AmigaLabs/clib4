/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for bsdsocket
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/lists.h>
#include <devices/timer.h>
#include <utility/tagitem.h>
#include <utility/hooks.h>
#include <netinet/in.h>
#include <sys/socket.h>
#include <sys/mbuf.h>
#include <net/route.h>
#include <netdb.h>
#include <libraries/bsdsocket.h>
#include <dos/dosextens.h>

#include <interfaces/bsdsocket.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct SocketIFace * ISocket = NULL;
static struct Library * __SocketBase = NULL;
static struct SocketIFace * __ISocket = NULL;

/****************************************************************************/

extern struct Library * SocketBase;

/****************************************************************************/

void bsdsocket_main_constructor(void)
{
    if (ISocket != NULL)
    {
        return; /* Someone was quicker */
    }
    if (SocketBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __SocketBase = SocketBase = IExec->OpenLibrary("bsdsocket.library", 0);
        if (SocketBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "bsdsocket.library");
            abort();
        }
    }

    __ISocket = ISocket = (struct SocketIFace *)IExec->GetInterface((struct Library *)SocketBase, "main", 1, NULL);
    if (ISocket == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "bsdsocket.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*bsdsocket_main_constructor_ptr)(void) USED = bsdsocket_main_constructor;

/****************************************************************************/

void bsdsocket_main_destructor(void)
{
    if (__ISocket)
    {
        IExec->DropInterface ((struct Interface *)__ISocket);
    }
    if (__SocketBase)
    {
        IExec->CloseLibrary(__SocketBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*bsdsocket_main_destructor_ptr)(void) USED = bsdsocket_main_destructor;

/****************************************************************************/

