/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for usbresource
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/types.h>
#include <exec/ports.h>
#include <utility/tagitem.h>

#include <interfaces/usbresource.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct USBResourceIFace * IUSBResource = NULL;
static struct Library * __USBResourceBase = NULL;
static struct USBResourceIFace * __IUSBResource = NULL;

/****************************************************************************/

extern struct Library * USBResourceBase;

/****************************************************************************/

void usbresource_main_constructor(void)
{
    if (IUSBResource != NULL)
    {
        return; /* Someone was quicker */
    }
    if (USBResourceBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __USBResourceBase = USBResourceBase = IExec->OpenLibrary("usbresource.library", 0);
        if (USBResourceBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "usbresource.library");
            abort();
        }
    }

    __IUSBResource = IUSBResource = (struct USBResourceIFace *)IExec->GetInterface((struct Library *)USBResourceBase, "main", 1, NULL);
    if (IUSBResource == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "usbresource.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*usbresource_main_constructor_ptr)(void) USED = usbresource_main_constructor;

/****************************************************************************/

void usbresource_main_destructor(void)
{
    if (__IUSBResource)
    {
        IExec->DropInterface ((struct Interface *)__IUSBResource);
    }
    if (__USBResourceBase)
    {
        IExec->CloseLibrary(__USBResourceBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*usbresource_main_destructor_ptr)(void) USED = usbresource_main_destructor;

/****************************************************************************/

