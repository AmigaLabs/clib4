/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for bevel
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <intuition/intuition.h>
#include <intuition/classes.h>

#include <interfaces/bevel.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct BevelIFace * IBevel = NULL;
static struct Library * __BevelBase = NULL;
static struct BevelIFace * __IBevel = NULL;

/****************************************************************************/

extern struct Library * BevelBase;

/****************************************************************************/

void bevel_main_constructor(void)
{
    if (IBevel != NULL)
    {
        return; /* Someone was quicker */
    }
    if (BevelBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __BevelBase = BevelBase = IExec->OpenLibrary("images/bevel.image", 0);
        if (BevelBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "images/bevel.image");
            abort();
        }
    }

    __IBevel = IBevel = (struct BevelIFace *)IExec->GetInterface((struct Library *)BevelBase, "main", 1, NULL);
    if (IBevel == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "images/bevel.image");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*bevel_main_constructor_ptr)(void) USED = bevel_main_constructor;

/****************************************************************************/

void bevel_main_destructor(void)
{
    if (__IBevel)
    {
        IExec->DropInterface ((struct Interface *)__IBevel);
    }
    if (__BevelBase)
    {
        IExec->CloseLibrary(__BevelBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*bevel_main_destructor_ptr)(void) USED = bevel_main_destructor;

/****************************************************************************/

