/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for usergroup
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <libraries/usergroup.h>
#include <pwd.h>
#include <grp.h>

#include <interfaces/usergroup.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct UserGroupIFace * IUserGroup = NULL;
static struct Library * __UserGroupBase = NULL;
static struct UserGroupIFace * __IUserGroup = NULL;

/****************************************************************************/

extern struct Library * UserGroupBase;

/****************************************************************************/

void usergroup_main_constructor(void)
{
    if (IUserGroup != NULL)
    {
        return; /* Someone was quicker */
    }
    if (UserGroupBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __UserGroupBase = UserGroupBase = IExec->OpenLibrary("usergroup.library", 0);
        if (UserGroupBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "usergroup.library");
            abort();
        }
    }

    __IUserGroup = IUserGroup = (struct UserGroupIFace *)IExec->GetInterface((struct Library *)UserGroupBase, "main", 1, NULL);
    if (IUserGroup == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "usergroup.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*usergroup_main_constructor_ptr)(void) USED = usergroup_main_constructor;

/****************************************************************************/

void usergroup_main_destructor(void)
{
    if (__IUserGroup)
    {
        IExec->DropInterface ((struct Interface *)__IUserGroup);
    }
    if (__UserGroupBase)
    {
        IExec->CloseLibrary(__UserGroupBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*usergroup_main_destructor_ptr)(void) USED = usergroup_main_destructor;

/****************************************************************************/

