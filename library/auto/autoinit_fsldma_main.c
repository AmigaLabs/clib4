/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for fsldma
**
**	Copyright (C) 2019,2021 Trevor Dickinson
**	All Rights Reserved.
*/

#include <interfaces/fsldma.h>
#include <resources/fsldma.h>

#include <interfaces/fsldma.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct fslDMAIFace * IfslDMA = NULL;
static struct Library * __fslDMABase = NULL;
static struct fslDMAIFace * __IfslDMA = NULL;

/****************************************************************************/

extern struct Library * fslDMABase;

/****************************************************************************/

void fsldma_main_constructor(void)
{
    if (IfslDMA != NULL)
    {
        return; /* Someone was quicker */
    }
    if (fslDMABase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __fslDMABase = fslDMABase = IExec->OpenLibrary("fsldma.resource", 0);
        if (fslDMABase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "fsldma.resource");
            abort();
        }
    }

    __IfslDMA = IfslDMA = (struct fslDMAIFace *)IExec->GetInterface((struct Library *)fslDMABase, "main", 1, NULL);
    if (IfslDMA == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "fsldma.resource");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*fsldma_main_constructor_ptr)(void) USED = fsldma_main_constructor;

/****************************************************************************/

void fsldma_main_destructor(void)
{
    if (__IfslDMA)
    {
        IExec->DropInterface ((struct Interface *)__IfslDMA);
    }
    if (__fslDMABase)
    {
        IExec->CloseLibrary(__fslDMABase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*fsldma_main_destructor_ptr)(void) USED = fsldma_main_destructor;

/****************************************************************************/

