/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for iffparse
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/types.h>
#include <libraries/iffparse.h>
#include <utility/hooks.h>

#include <interfaces/iffparse.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct IFFParseIFace * IIFFParse = NULL;
static struct Library * __IFFParseBase = NULL;
static struct IFFParseIFace * __IIFFParse = NULL;

/****************************************************************************/

extern struct Library * IFFParseBase;

/****************************************************************************/

void iffparse_main_constructor(void)
{
    if (IIFFParse != NULL)
    {
        return; /* Someone was quicker */
    }
    if (IFFParseBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __IFFParseBase = IFFParseBase = IExec->OpenLibrary("iffparse.library", 0);
        if (IFFParseBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "iffparse.library");
            abort();
        }
    }

    __IIFFParse = IIFFParse = (struct IFFParseIFace *)IExec->GetInterface((struct Library *)IFFParseBase, "main", 1, NULL);
    if (IIFFParse == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "iffparse.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*iffparse_main_constructor_ptr)(void) USED = iffparse_main_constructor;

/****************************************************************************/

void iffparse_main_destructor(void)
{
    if (__IIFFParse)
    {
        IExec->DropInterface ((struct Interface *)__IIFFParse);
    }
    if (__IFFParseBase)
    {
        IExec->CloseLibrary(__IFFParseBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*iffparse_main_destructor_ptr)(void) USED = iffparse_main_destructor;

/****************************************************************************/

