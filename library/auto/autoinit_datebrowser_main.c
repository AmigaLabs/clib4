/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for datebrowser
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <intuition/intuition.h>
#include <intuition/classes.h>

#include <interfaces/datebrowser.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <proto/intuition.h>
#include <stdlib.h>

static struct Library * __IntuitionBase = NULL;
static struct IntuitionIFace *__IIntuition = NULL;
struct DateBrowserIFace * IDateBrowser = NULL;
static struct ClassLibrary * __DateBrowserBase = NULL;
static struct DateBrowserIFace * __IDateBrowser = NULL;

/****************************************************************************/

extern struct ClassLibrary * DateBrowserBase;
extern Class * DateBrowserClass;

/****************************************************************************/

void datebrowser_main_constructor(void)
{
    if (IDateBrowser != NULL)
    {
        return; /* Someone was quicker */
    }
    if (DateBrowserBase == NULL) /* Library base is NULL, we need to open it */
    {
        if (IIntuition == NULL)
        {
            if (IntuitionBase == NULL)
            {
                __IntuitionBase = IntuitionBase = IExec->OpenLibrary("intuition.library", 0);
                if (IntuitionBase == NULL)
                {
                    BPTR StdErr = IDOS->ErrorOutput();
                    if (StdErr == ZERO)
                        StdErr = IDOS->Output();
                    IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "intuition.library");
                    abort();
                }
            }
            __IIntuition = IIntuition = (struct IntuitionIFace *)IExec->GetInterface(IntuitionBase, "main", 1, NULL);
            if (IIntuition == NULL)
            {
                BPTR StdErr = IDOS->ErrorOutput();
                if (StdErr == ZERO)
                    StdErr = IDOS->Output();
                IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "intuition.library");
                abort();
            }
        }
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __DateBrowserBase = DateBrowserBase = IIntuition->OpenClass("gadgets/datebrowser.gadget", 0, &DateBrowserClass);
        if (DateBrowserBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "gadgets/datebrowser.gadget");
            abort();
        }
    }

    __IDateBrowser = IDateBrowser = (struct DateBrowserIFace *)IExec->GetInterface((struct Library *)DateBrowserBase, "main", 1, NULL);
    if (IDateBrowser == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "gadgets/datebrowser.gadget");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*datebrowser_main_constructor_ptr)(void) USED = datebrowser_main_constructor;

/****************************************************************************/

void datebrowser_main_destructor(void)
{
    if (__IDateBrowser)
    {
        IExec->DropInterface ((struct Interface *)__IDateBrowser);
    }
    if (__DateBrowserBase != NULL)
    {
        IIntuition->CloseClass(__DateBrowserBase);
    }
    if (__IIntuition != NULL)
    {
        IExec->DropInterface((struct Interface *)__IIntuition);
    }
    if (__IntuitionBase != NULL)
    {
        IExec->CloseLibrary(__IntuitionBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*datebrowser_main_destructor_ptr)(void) USED = datebrowser_main_destructor;

/****************************************************************************/

