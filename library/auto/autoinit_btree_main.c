/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for btree
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/types.h>
#include <libraries/btree.h>

#include <interfaces/btree.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct BTreeIFace * IBTree = NULL;
static struct Library * __BTreeBase = NULL;
static struct BTreeIFace * __IBTree = NULL;

/****************************************************************************/

extern struct Library * BTreeBase;

/****************************************************************************/

void btree_main_constructor(void)
{
    if (IBTree != NULL)
    {
        return; /* Someone was quicker */
    }
    if (BTreeBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __BTreeBase = BTreeBase = IExec->OpenLibrary("btree.library", 0);
        if (BTreeBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "btree.library");
            abort();
        }
    }

    __IBTree = IBTree = (struct BTreeIFace *)IExec->GetInterface((struct Library *)BTreeBase, "main", 1, NULL);
    if (IBTree == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "btree.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*btree_main_constructor_ptr)(void) USED = btree_main_constructor;

/****************************************************************************/

void btree_main_destructor(void)
{
    if (__IBTree)
    {
        IExec->DropInterface ((struct Interface *)__IBTree);
    }
    if (__BTreeBase)
    {
        IExec->CloseLibrary(__BTreeBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*btree_main_destructor_ptr)(void) USED = btree_main_destructor;

/****************************************************************************/

