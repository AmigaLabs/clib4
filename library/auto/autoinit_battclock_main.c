/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for battclock
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/types.h>

#include <interfaces/battclock.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct BattClockIFace * IBattClock = NULL;
static struct Library * __BattClockBase = NULL;
static struct BattClockIFace * __IBattClock = NULL;

/****************************************************************************/

extern struct Library * BattClockBase;

/****************************************************************************/

void battclock_main_constructor(void)
{
    if (IBattClock != NULL)
    {
        return; /* Someone was quicker */
    }
    if (BattClockBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __BattClockBase = BattClockBase = IExec->OpenLibrary("battclock.resource", 0);
        if (BattClockBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "battclock.resource");
            abort();
        }
    }

    __IBattClock = IBattClock = (struct BattClockIFace *)IExec->GetInterface((struct Library *)BattClockBase, "main", 1, NULL);
    if (IBattClock == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "battclock.resource");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*battclock_main_constructor_ptr)(void) USED = battclock_main_constructor;

/****************************************************************************/

void battclock_main_destructor(void)
{
    if (__IBattClock)
    {
        IExec->DropInterface ((struct Interface *)__IBattClock);
    }
    if (__BattClockBase)
    {
        IExec->CloseLibrary(__BattClockBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*battclock_main_destructor_ptr)(void) USED = battclock_main_destructor;

/****************************************************************************/

