/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for getfile
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <intuition/intuition.h>
#include <intuition/classes.h>

#include <interfaces/getfile.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct GetFileIFace * IGetFile = NULL;
static struct Library * __GetFileBase = NULL;
static struct GetFileIFace * __IGetFile = NULL;

/****************************************************************************/

extern struct Library * GetFileBase;

/****************************************************************************/

void getfile_main_constructor(void)
{
    if (IGetFile != NULL)
    {
        return; /* Someone was quicker */
    }
    if (GetFileBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __GetFileBase = GetFileBase = IExec->OpenLibrary("gadgets/getfile.gadget", 0);
        if (GetFileBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "gadgets/getfile.gadget");
            abort();
        }
    }

    __IGetFile = IGetFile = (struct GetFileIFace *)IExec->GetInterface((struct Library *)GetFileBase, "main", 1, NULL);
    if (IGetFile == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "gadgets/getfile.gadget");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*getfile_main_constructor_ptr)(void) USED = getfile_main_constructor;

/****************************************************************************/

void getfile_main_destructor(void)
{
    if (__IGetFile)
    {
        IExec->DropInterface ((struct Interface *)__IGetFile);
    }
    if (__GetFileBase)
    {
        IExec->CloseLibrary(__GetFileBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*getfile_main_destructor_ptr)(void) USED = getfile_main_destructor;

/****************************************************************************/

