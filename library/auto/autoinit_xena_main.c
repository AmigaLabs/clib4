/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for xena
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/


#include <interfaces/xena.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct XenaIFace * IXena = NULL;
static struct Library * __XenaBase = NULL;
static struct XenaIFace * __IXena = NULL;

/****************************************************************************/

extern struct Library * XenaBase;

/****************************************************************************/

void xena_main_constructor(void)
{
    if (IXena != NULL)
    {
        return; /* Someone was quicker */
    }
    if (XenaBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __XenaBase = XenaBase = IExec->OpenLibrary("xena.resource", 0);
        if (XenaBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "xena.resource");
            abort();
        }
    }

    __IXena = IXena = (struct XenaIFace *)IExec->GetInterface((struct Library *)XenaBase, "main", 1, NULL);
    if (IXena == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "xena.resource");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*xena_main_constructor_ptr)(void) USED = xena_main_constructor;

/****************************************************************************/

void xena_main_destructor(void)
{
    if (__IXena)
    {
        IExec->DropInterface ((struct Interface *)__IXena);
    }
    if (__XenaBase)
    {
        IExec->CloseLibrary(__XenaBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*xena_main_destructor_ptr)(void) USED = xena_main_destructor;

/****************************************************************************/

