/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for bzip2
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <libraries/bzip2.h>
#include <stdio.h>

#include <interfaces/bzip2.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct BZip2IFace * IBZip2 = NULL;
static struct Library * __BZip2Base = NULL;
static struct BZip2IFace * __IBZip2 = NULL;

/****************************************************************************/

extern struct Library * BZip2Base;

/****************************************************************************/

void bzip2_main_constructor(void)
{
    if (IBZip2 != NULL)
    {
        return; /* Someone was quicker */
    }
    if (BZip2Base == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __BZip2Base = BZip2Base = IExec->OpenLibrary("bzip2.library", 0);
        if (BZip2Base == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "bzip2.library");
            abort();
        }
    }

    __IBZip2 = IBZip2 = (struct BZip2IFace *)IExec->GetInterface((struct Library *)BZip2Base, "main", 1, NULL);
    if (IBZip2 == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "bzip2.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*bzip2_main_constructor_ptr)(void) USED = bzip2_main_constructor;

/****************************************************************************/

void bzip2_main_destructor(void)
{
    if (__IBZip2)
    {
        IExec->DropInterface ((struct Interface *)__IBZip2);
    }
    if (__BZip2Base)
    {
        IExec->CloseLibrary(__BZip2Base);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*bzip2_main_destructor_ptr)(void) USED = bzip2_main_destructor;

/****************************************************************************/

