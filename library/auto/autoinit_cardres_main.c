/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for cardres
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/types.h>
#include <exec/resident.h>
#include <resources/card.h>

#include <interfaces/cardres.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct CardIFace * ICard = NULL;
static struct Library * __CardResource = NULL;
static struct CardIFace * __ICard = NULL;

/****************************************************************************/

extern struct Library * CardResource;

/****************************************************************************/

void cardres_main_constructor(void)
{
    if (ICard != NULL)
    {
        return; /* Someone was quicker */
    }
    if (CardResource == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __CardResource = CardResource = IExec->OpenLibrary("include/cardres.library", 0);
        if (CardResource == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "include/cardres.library");
            abort();
        }
    }

    __ICard = ICard = (struct CardIFace *)IExec->GetInterface((struct Library *)CardResource, "main", 1, NULL);
    if (ICard == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "include/cardres.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*cardres_main_constructor_ptr)(void) USED = cardres_main_constructor;

/****************************************************************************/

void cardres_main_destructor(void)
{
    if (__ICard)
    {
        IExec->DropInterface ((struct Interface *)__ICard);
    }
    if (__CardResource)
    {
        IExec->CloseLibrary(__CardResource);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*cardres_main_destructor_ptr)(void) USED = cardres_main_destructor;

/****************************************************************************/

