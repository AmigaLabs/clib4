/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for performancemonitor
**
**	Copyright (C) 2004 Trevor Dickinson
**	All Rights Reserved.
*/

#include <interfaces/performancemonitor.h>
#include <resources/performancemonitor.h>

#include <interfaces/performancemonitor.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct PerformanceMonitorIFace * IPM = NULL;
static struct Library * __PMBase = NULL;
static struct PerformanceMonitorIFace * __IPM = NULL;

/****************************************************************************/

extern struct Library * PMBase;

/****************************************************************************/

void performancemonitor_main_constructor(void)
{
    if (IPM != NULL)
    {
        return; /* Someone was quicker */
    }
    if (PMBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __PMBase = PMBase = IExec->OpenLibrary("performancemonitor.resource", 0);
        if (PMBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "performancemonitor.resource");
            abort();
        }
    }

    __IPM = IPM = (struct PerformanceMonitorIFace *)IExec->GetInterface((struct Library *)PMBase, "main", 1, NULL);
    if (IPM == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "performancemonitor.resource");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*performancemonitor_main_constructor_ptr)(void) USED = performancemonitor_main_constructor;

/****************************************************************************/

void performancemonitor_main_destructor(void)
{
    if (__IPM)
    {
        IExec->DropInterface ((struct Interface *)__IPM);
    }
    if (__PMBase)
    {
        IExec->CloseLibrary(__PMBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*performancemonitor_main_destructor_ptr)(void) USED = performancemonitor_main_destructor;

/****************************************************************************/

