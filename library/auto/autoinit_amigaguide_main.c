/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for amigaguide
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/types.h>
#include <exec/ports.h>
#include <dos/dos.h>
#include <libraries/amigaguide.h>
#include <utility/tagitem.h>
#include <utility/hooks.h>
#include <rexx/storage.h>

#include <interfaces/amigaguide.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct AmigaGuideIFace * IAmigaGuide = NULL;
static struct Library * __AmigaGuideBase = NULL;
static struct AmigaGuideIFace * __IAmigaGuide = NULL;

/****************************************************************************/

extern struct Library * AmigaGuideBase;

/****************************************************************************/

void amigaguide_main_constructor(void)
{
    if (IAmigaGuide != NULL)
    {
        return; /* Someone was quicker */
    }
    if (AmigaGuideBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __AmigaGuideBase = AmigaGuideBase = IExec->OpenLibrary("amigaguide.library", 0);
        if (AmigaGuideBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "amigaguide.library");
            abort();
        }
    }

    __IAmigaGuide = IAmigaGuide = (struct AmigaGuideIFace *)IExec->GetInterface((struct Library *)AmigaGuideBase, "main", 1, NULL);
    if (IAmigaGuide == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "amigaguide.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*amigaguide_main_constructor_ptr)(void) USED = amigaguide_main_constructor;

/****************************************************************************/

void amigaguide_main_destructor(void)
{
    if (__IAmigaGuide)
    {
        IExec->DropInterface ((struct Interface *)__IAmigaGuide);
    }
    if (__AmigaGuideBase)
    {
        IExec->CloseLibrary(__AmigaGuideBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*amigaguide_main_destructor_ptr)(void) USED = amigaguide_main_destructor;

/****************************************************************************/

