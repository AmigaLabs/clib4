/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for virtual
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <intuition/intuition.h>
#include <intuition/classes.h>
#include <intuition/classusr.h>
#include <gadgets/layout.h>

#include <interfaces/virtual.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct VirtualIFace * IVirtual = NULL;
static struct Library * __VirtualBase = NULL;
static struct VirtualIFace * __IVirtual = NULL;

/****************************************************************************/

extern struct Library * VirtualBase;

/****************************************************************************/

void virtual_main_constructor(void)
{
    if (IVirtual != NULL)
    {
        return; /* Someone was quicker */
    }
    if (VirtualBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __VirtualBase = VirtualBase = IExec->OpenLibrary("gadgets/virtual.gadget", 0);
        if (VirtualBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "gadgets/virtual.gadget");
            abort();
        }
    }

    __IVirtual = IVirtual = (struct VirtualIFace *)IExec->GetInterface((struct Library *)VirtualBase, "main", 1, NULL);
    if (IVirtual == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "gadgets/virtual.gadget");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*virtual_main_constructor_ptr)(void) USED = virtual_main_constructor;

/****************************************************************************/

void virtual_main_destructor(void)
{
    if (__IVirtual)
    {
        IExec->DropInterface ((struct Interface *)__IVirtual);
    }
    if (__VirtualBase)
    {
        IExec->CloseLibrary(__VirtualBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*virtual_main_destructor_ptr)(void) USED = virtual_main_destructor;

/****************************************************************************/

