/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for dos
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <dos/dos.h>
#include <dos/dosextens.h>
#include <dos/record.h>
#include <dos/rdargs.h>
#include <dos/dosasl.h>
#include <dos/var.h>
#include <dos/notify.h>
#include <dos/datetime.h>
#include <dos/exall.h>
#include <dos/filehandler.h>
#include <dos/dostags.h>
#include <dos/mount.h>
#include <dos/doshunks.h>
#include <dos/path.h>
#include <dos/anchorpath.h>

#include <interfaces/dos.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct DOSIFace * IDOS = NULL;
static struct Library * __DOSBase = NULL;
static struct DOSIFace * __IDOS = NULL;

/****************************************************************************/

extern struct Library * DOSBase;

/****************************************************************************/

void dos_main_constructor(void)
{
    if (IDOS != NULL)
    {
        return; /* Someone was quicker */
    }
    if (DOSBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __DOSBase = DOSBase = IExec->OpenLibrary("dos.library", 0);
        if (DOSBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "dos.library");
            abort();
        }
    }

    __IDOS = IDOS = (struct DOSIFace *)IExec->GetInterface((struct Library *)DOSBase, "main", 1, NULL);
    if (IDOS == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "dos.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*dos_main_constructor_ptr)(void) USED = dos_main_constructor;

/****************************************************************************/

void dos_main_destructor(void)
{
    if (__IDOS)
    {
        IExec->DropInterface ((struct Interface *)__IDOS);
    }
    if (__DOSBase)
    {
        IExec->CloseLibrary(__DOSBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*dos_main_destructor_ptr)(void) USED = dos_main_destructor;

/****************************************************************************/

