/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for battmem
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/types.h>

#include <interfaces/battmem.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct BattMemIFace * IBattMem = NULL;
static struct Library * __BattMemBase = NULL;
static struct BattMemIFace * __IBattMem = NULL;

/****************************************************************************/

extern struct Library * BattMemBase;

/****************************************************************************/

void battmem_main_constructor(void)
{
    if (IBattMem != NULL)
    {
        return; /* Someone was quicker */
    }
    if (BattMemBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __BattMemBase = BattMemBase = IExec->OpenLibrary("battmem.resource", 0);
        if (BattMemBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "battmem.resource");
            abort();
        }
    }

    __IBattMem = IBattMem = (struct BattMemIFace *)IExec->GetInterface((struct Library *)BattMemBase, "main", 1, NULL);
    if (IBattMem == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "battmem.resource");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*battmem_main_constructor_ptr)(void) USED = battmem_main_constructor;

/****************************************************************************/

void battmem_main_destructor(void)
{
    if (__IBattMem)
    {
        IExec->DropInterface ((struct Interface *)__IBattMem);
    }
    if (__BattMemBase)
    {
        IExec->CloseLibrary(__BattMemBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*battmem_main_destructor_ptr)(void) USED = battmem_main_destructor;

/****************************************************************************/

