/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for bootsd
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/


#include <interfaces/bootsd.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct BootSDIFace * IBootSD = NULL;
static struct Library * __BootSDBase = NULL;
static struct BootSDIFace * __IBootSD = NULL;

/****************************************************************************/

extern struct Library * BootSDBase;

/****************************************************************************/

void bootsd_main_constructor(void)
{
    if (IBootSD != NULL)
    {
        return; /* Someone was quicker */
    }
    if (BootSDBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __BootSDBase = BootSDBase = IExec->OpenLibrary("bootsd.resource", 0);
        if (BootSDBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "bootsd.resource");
            abort();
        }
    }

    __IBootSD = IBootSD = (struct BootSDIFace *)IExec->GetInterface((struct Library *)BootSDBase, "main", 1, NULL);
    if (IBootSD == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "bootsd.resource");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*bootsd_main_constructor_ptr)(void) USED = bootsd_main_constructor;

/****************************************************************************/

void bootsd_main_destructor(void)
{
    if (__IBootSD)
    {
        IExec->DropInterface ((struct Interface *)__IBootSD);
    }
    if (__BootSDBase)
    {
        IExec->CloseLibrary(__BootSDBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*bootsd_main_destructor_ptr)(void) USED = bootsd_main_destructor;

/****************************************************************************/

