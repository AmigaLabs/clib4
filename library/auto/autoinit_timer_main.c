/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for timer
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <devices/timer.h>

#include <interfaces/timer.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct TimerIFace * ITimer = NULL;
static struct Library * __TimerBase = NULL;
static struct TimerIFace * __ITimer = NULL;

/****************************************************************************/

extern struct Library * TimerBase;

/****************************************************************************/

void timer_main_constructor(void)
{
    if (ITimer != NULL)
    {
        return; /* Someone was quicker */
    }
    if (TimerBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __TimerBase = TimerBase = IExec->OpenLibrary("timer.device", 0);
        if (TimerBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "timer.device");
            abort();
        }
    }

    __ITimer = ITimer = (struct TimerIFace *)IExec->GetInterface((struct Library *)TimerBase, "main", 1, NULL);
    if (ITimer == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "timer.device");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*timer_main_constructor_ptr)(void) USED = timer_main_constructor;

/****************************************************************************/

void timer_main_destructor(void)
{
    if (__ITimer)
    {
        IExec->DropInterface ((struct Interface *)__ITimer);
    }
    if (__TimerBase)
    {
        IExec->CloseLibrary(__TimerBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*timer_main_destructor_ptr)(void) USED = timer_main_destructor;

/****************************************************************************/

