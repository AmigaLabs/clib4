/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for screenblanker
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/libraries.h>
#include <proto/blankermodule.h>

#include <interfaces/screenblanker.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct ScreenBlankerIFace * IScreenBlanker = NULL;
static struct Library * __ScreenBlanker = NULL;
static struct ScreenBlankerIFace * __IScreenBlanker = NULL;

/****************************************************************************/

extern struct Library * ScreenBlanker;

/****************************************************************************/

void screenblanker_main_constructor(void)
{
    if (IScreenBlanker != NULL)
    {
        return; /* Someone was quicker */
    }
    if (ScreenBlanker == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __ScreenBlanker = ScreenBlanker = IExec->OpenLibrary("screenblanker.library", 0);
        if (ScreenBlanker == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "screenblanker.library");
            abort();
        }
    }

    __IScreenBlanker = IScreenBlanker = (struct ScreenBlankerIFace *)IExec->GetInterface((struct Library *)ScreenBlanker, "main", 1, NULL);
    if (IScreenBlanker == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "screenblanker.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*screenblanker_main_constructor_ptr)(void) USED = screenblanker_main_constructor;

/****************************************************************************/

void screenblanker_main_destructor(void)
{
    if (__IScreenBlanker)
    {
        IExec->DropInterface ((struct Interface *)__IScreenBlanker);
    }
    if (__ScreenBlanker)
    {
        IExec->CloseLibrary(__ScreenBlanker);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*screenblanker_main_destructor_ptr)(void) USED = screenblanker_main_destructor;

/****************************************************************************/

