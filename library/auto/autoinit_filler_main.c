/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for filler
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <intuition/intuition.h>
#include <intuition/classes.h>

#include <interfaces/filler.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct FillerIFace * IFiller = NULL;
static struct Library * __FillerBase = NULL;
static struct FillerIFace * __IFiller = NULL;

/****************************************************************************/

extern struct Library * FillerBase;

/****************************************************************************/

void filler_main_constructor(void)
{
    if (IFiller != NULL)
    {
        return; /* Someone was quicker */
    }
    if (FillerBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __FillerBase = FillerBase = IExec->OpenLibrary("images/filler.image", 0);
        if (FillerBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "images/filler.image");
            abort();
        }
    }

    __IFiller = IFiller = (struct FillerIFace *)IExec->GetInterface((struct Library *)FillerBase, "main", 1, NULL);
    if (IFiller == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "images/filler.image");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*filler_main_constructor_ptr)(void) USED = filler_main_constructor;

/****************************************************************************/

void filler_main_destructor(void)
{
    if (__IFiller)
    {
        IExec->DropInterface ((struct Interface *)__IFiller);
    }
    if (__FillerBase)
    {
        IExec->CloseLibrary(__FillerBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*filler_main_destructor_ptr)(void) USED = filler_main_destructor;

/****************************************************************************/

