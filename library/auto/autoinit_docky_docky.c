/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for docky
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/libraries.h>

#include <interfaces/docky.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct DockyIFace * IDocky = NULL;
static struct Library * __Docky = NULL;
static struct DockyIFace * __IDocky = NULL;

/****************************************************************************/

extern struct Library * Docky;

/****************************************************************************/

void docky_docky_constructor(void)
{
    if (IDocky != NULL)
    {
        return; /* Someone was quicker */
    }
    if (Docky == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __Docky = Docky = IExec->OpenLibrary("docky.library", 0);
        if (Docky == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "docky.library");
            abort();
        }
    }

    __IDocky = IDocky = (struct DockyIFace *)IExec->GetInterface((struct Library *)Docky, "docky", 1, NULL);
    if (IDocky == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "docky", 1, "docky.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*docky_docky_constructor_ptr)(void) USED = docky_docky_constructor;

/****************************************************************************/

void docky_docky_destructor(void)
{
    if (__IDocky)
    {
        IExec->DropInterface ((struct Interface *)__IDocky);
    }
    if (__Docky)
    {
        IExec->CloseLibrary(__Docky);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*docky_docky_destructor_ptr)(void) USED = docky_docky_destructor;

/****************************************************************************/

