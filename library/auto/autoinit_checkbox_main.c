/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for checkbox
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <intuition/intuition.h>
#include <intuition/classes.h>

#include <interfaces/checkbox.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <proto/intuition.h>
#include <stdlib.h>

static struct Library * __IntuitionBase = NULL;
static struct IntuitionIFace *__IIntuition = NULL;
struct CheckBoxIFace * ICheckBox = NULL;
static struct ClassLibrary * __CheckBoxBase = NULL;
static struct CheckBoxIFace * __ICheckBox = NULL;

/****************************************************************************/

extern struct ClassLibrary * CheckBoxBase;
extern Class * CheckBoxClass;

/****************************************************************************/

void checkbox_main_constructor(void)
{
    if (ICheckBox != NULL)
    {
        return; /* Someone was quicker */
    }
    if (CheckBoxBase == NULL) /* Library base is NULL, we need to open it */
    {
        if (IIntuition == NULL)
        {
            if (IntuitionBase == NULL)
            {
                __IntuitionBase = IntuitionBase = IExec->OpenLibrary("intuition.library", 0);
                if (IntuitionBase == NULL)
                {
                    BPTR StdErr = IDOS->ErrorOutput();
                    if (StdErr == ZERO)
                        StdErr = IDOS->Output();
                    IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "intuition.library");
                    abort();
                }
            }
            __IIntuition = IIntuition = (struct IntuitionIFace *)IExec->GetInterface(IntuitionBase, "main", 1, NULL);
            if (IIntuition == NULL)
            {
                BPTR StdErr = IDOS->ErrorOutput();
                if (StdErr == ZERO)
                    StdErr = IDOS->Output();
                IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "intuition.library");
                abort();
            }
        }
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __CheckBoxBase = CheckBoxBase = IIntuition->OpenClass("gadgets/checkbox.gadget", 0, &CheckBoxClass);
        if (CheckBoxBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "gadgets/checkbox.gadget");
            abort();
        }
    }

    __ICheckBox = ICheckBox = (struct CheckBoxIFace *)IExec->GetInterface((struct Library *)CheckBoxBase, "main", 1, NULL);
    if (ICheckBox == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "gadgets/checkbox.gadget");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*checkbox_main_constructor_ptr)(void) USED = checkbox_main_constructor;

/****************************************************************************/

void checkbox_main_destructor(void)
{
    if (__ICheckBox)
    {
        IExec->DropInterface ((struct Interface *)__ICheckBox);
    }
    if (__CheckBoxBase != NULL)
    {
        IIntuition->CloseClass(__CheckBoxBase);
    }
    if (__IIntuition != NULL)
    {
        IExec->DropInterface((struct Interface *)__IIntuition);
    }
    if (__IntuitionBase != NULL)
    {
        IExec->CloseLibrary(__IntuitionBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*checkbox_main_destructor_ptr)(void) USED = checkbox_main_destructor;

/****************************************************************************/

