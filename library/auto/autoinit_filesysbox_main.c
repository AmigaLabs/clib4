/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for filesysbox
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/types.h>
#include <utility/tagitem.h>
#include <libraries/filesysbox.h>
#include <devices/timer.h>

#include <interfaces/filesysbox.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct FileSysBoxIFace * IFileSysBox = NULL;
static struct Library * __FileSysBoxBase = NULL;
static struct FileSysBoxIFace * __IFileSysBox = NULL;

/****************************************************************************/

extern struct Library * FileSysBoxBase;

/****************************************************************************/

void filesysbox_main_constructor(void)
{
    if (IFileSysBox != NULL)
    {
        return; /* Someone was quicker */
    }
    if (FileSysBoxBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __FileSysBoxBase = FileSysBoxBase = IExec->OpenLibrary("filesysbox.library", 0);
        if (FileSysBoxBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "filesysbox.library");
            abort();
        }
    }

    __IFileSysBox = IFileSysBox = (struct FileSysBoxIFace *)IExec->GetInterface((struct Library *)FileSysBoxBase, "main", 1, NULL);
    if (IFileSysBox == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "filesysbox.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*filesysbox_main_constructor_ptr)(void) USED = filesysbox_main_constructor;

/****************************************************************************/

void filesysbox_main_destructor(void)
{
    if (__IFileSysBox)
    {
        IExec->DropInterface ((struct Interface *)__IFileSysBox);
    }
    if (__FileSysBoxBase)
    {
        IExec->CloseLibrary(__FileSysBoxBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*filesysbox_main_destructor_ptr)(void) USED = filesysbox_main_destructor;

/****************************************************************************/

