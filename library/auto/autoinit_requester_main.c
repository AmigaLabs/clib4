/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for requester
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <intuition/intuition.h>
#include <intuition/classes.h>

#include <interfaces/requester.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <proto/intuition.h>
#include <stdlib.h>

static struct Library * __IntuitionBase = NULL;
static struct IntuitionIFace *__IIntuition = NULL;
struct RequesterIFace * IRequester = NULL;
static struct ClassLibrary * __RequesterBase = NULL;
static struct RequesterIFace * __IRequester = NULL;

/****************************************************************************/

extern struct ClassLibrary * RequesterBase;
extern Class * RequesterClass;

/****************************************************************************/

void requester_main_constructor(void)
{
    if (IRequester != NULL)
    {
        return; /* Someone was quicker */
    }
    if (RequesterBase == NULL) /* Library base is NULL, we need to open it */
    {
        if (IIntuition == NULL)
        {
            if (IntuitionBase == NULL)
            {
                __IntuitionBase = IntuitionBase = IExec->OpenLibrary("intuition.library", 0);
                if (IntuitionBase == NULL)
                {
                    BPTR StdErr = IDOS->ErrorOutput();
                    if (StdErr == ZERO)
                        StdErr = IDOS->Output();
                    IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "intuition.library");
                    abort();
                }
            }
            __IIntuition = IIntuition = (struct IntuitionIFace *)IExec->GetInterface(IntuitionBase, "main", 1, NULL);
            if (IIntuition == NULL)
            {
                BPTR StdErr = IDOS->ErrorOutput();
                if (StdErr == ZERO)
                    StdErr = IDOS->Output();
                IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "intuition.library");
                abort();
            }
        }
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __RequesterBase = RequesterBase = IIntuition->OpenClass("requester.class", 0, &RequesterClass);
        if (RequesterBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "requester.class");
            abort();
        }
    }

    __IRequester = IRequester = (struct RequesterIFace *)IExec->GetInterface((struct Library *)RequesterBase, "main", 1, NULL);
    if (IRequester == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "requester.class");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*requester_main_constructor_ptr)(void) USED = requester_main_constructor;

/****************************************************************************/

void requester_main_destructor(void)
{
    if (__IRequester)
    {
        IExec->DropInterface ((struct Interface *)__IRequester);
    }
    if (__RequesterBase != NULL)
    {
        IIntuition->CloseClass(__RequesterBase);
    }
    if (__IIntuition != NULL)
    {
        IExec->DropInterface((struct Interface *)__IIntuition);
    }
    if (__IntuitionBase != NULL)
    {
        IExec->CloseLibrary(__IntuitionBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*requester_main_destructor_ptr)(void) USED = requester_main_destructor;

/****************************************************************************/

