/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for application
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/libraries.h>
#include <libraries/application.h>

#include <interfaces/application.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct PrefsObjectsIFace * IPrefsObjects = NULL;
static struct Library * __ApplicationBase = NULL;
static struct PrefsObjectsIFace * __IPrefsObjects = NULL;

/****************************************************************************/

extern struct Library * ApplicationBase;

/****************************************************************************/

void application_prefsobjects_constructor(void)
{
    if (IPrefsObjects != NULL)
    {
        return; /* Someone was quicker */
    }
    if (ApplicationBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __ApplicationBase = ApplicationBase = IExec->OpenLibrary("application.library", 0);
        if (ApplicationBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "application.library");
            abort();
        }
    }

    __IPrefsObjects = IPrefsObjects = (struct PrefsObjectsIFace *)IExec->GetInterface((struct Library *)ApplicationBase, "prefsobjects", 2, NULL);
    if (IPrefsObjects == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "prefsobjects", 2, "application.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*application_prefsobjects_constructor_ptr)(void) USED = application_prefsobjects_constructor;

/****************************************************************************/

void application_prefsobjects_destructor(void)
{
    if (__IPrefsObjects)
    {
        IExec->DropInterface ((struct Interface *)__IPrefsObjects);
    }
    if (__ApplicationBase)
    {
        IExec->CloseLibrary(__ApplicationBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*application_prefsobjects_destructor_ptr)(void) USED = application_prefsobjects_destructor;

/****************************************************************************/

