/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for asl
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/libraries.h>
#include <utility/tagitem.h>
#include <libraries/asl.h>

#include <interfaces/asl.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct AslIFace * IAsl = NULL;
static struct Library * __AslBase = NULL;
static struct AslIFace * __IAsl = NULL;

/****************************************************************************/

extern struct Library * AslBase;

/****************************************************************************/

void asl_main_constructor(void)
{
    if (IAsl != NULL)
    {
        return; /* Someone was quicker */
    }
    if (AslBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __AslBase = AslBase = IExec->OpenLibrary("asl.library", 0);
        if (AslBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "asl.library");
            abort();
        }
    }

    __IAsl = IAsl = (struct AslIFace *)IExec->GetInterface((struct Library *)AslBase, "main", 1, NULL);
    if (IAsl == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "asl.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*asl_main_constructor_ptr)(void) USED = asl_main_constructor;

/****************************************************************************/

void asl_main_destructor(void)
{
    if (__IAsl)
    {
        IExec->DropInterface ((struct Interface *)__IAsl);
    }
    if (__AslBase)
    {
        IExec->CloseLibrary(__AslBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*asl_main_destructor_ptr)(void) USED = asl_main_destructor;

/****************************************************************************/

