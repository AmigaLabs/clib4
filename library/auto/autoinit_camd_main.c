/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for camd
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/


#include <interfaces/camd.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct CamdIFace * ICamd = NULL;
static struct Library * __CamdBase = NULL;
static struct CamdIFace * __ICamd = NULL;

/****************************************************************************/

extern struct Library * CamdBase;

/****************************************************************************/

void camd_main_constructor(void)
{
    if (ICamd != NULL)
    {
        return; /* Someone was quicker */
    }
    if (CamdBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __CamdBase = CamdBase = IExec->OpenLibrary("camd.library", 0);
        if (CamdBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "camd.library");
            abort();
        }
    }

    __ICamd = ICamd = (struct CamdIFace *)IExec->GetInterface((struct Library *)CamdBase, "main", 1, NULL);
    if (ICamd == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "camd.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*camd_main_constructor_ptr)(void) USED = camd_main_constructor;

/****************************************************************************/

void camd_main_destructor(void)
{
    if (__ICamd)
    {
        IExec->DropInterface ((struct Interface *)__ICamd);
    }
    if (__CamdBase)
    {
        IExec->CloseLibrary(__CamdBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*camd_main_destructor_ptr)(void) USED = camd_main_destructor;

/****************************************************************************/

