/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for utility
**
**	Copyright (C) 2003-2021 Trevor Dickinson
**	All Rights Reserved.
*/

#include <utility/utility.h>

#include <interfaces/utility.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct UtilityIFace * IUtility = NULL;
static struct Library * __UtilityBase = NULL;
static struct UtilityIFace * __IUtility = NULL;

/****************************************************************************/

extern struct Library * UtilityBase;

/****************************************************************************/

void utility_main_constructor(void)
{
    if (IUtility != NULL)
    {
        return; /* Someone was quicker */
    }
    if (UtilityBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __UtilityBase = UtilityBase = IExec->OpenLibrary("utility.library", 0);
        if (UtilityBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "utility.library");
            abort();
        }
    }

    __IUtility = IUtility = (struct UtilityIFace *)IExec->GetInterface((struct Library *)UtilityBase, "main", 1, NULL);
    if (IUtility == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "utility.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*utility_main_constructor_ptr)(void) USED = utility_main_constructor;

/****************************************************************************/

void utility_main_destructor(void)
{
    if (__IUtility)
    {
        IExec->DropInterface ((struct Interface *)__IUtility);
    }
    if (__UtilityBase)
    {
        IExec->CloseLibrary(__UtilityBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*utility_main_destructor_ptr)(void) USED = utility_main_destructor;

/****************************************************************************/

