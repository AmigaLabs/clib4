/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for drawlist
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <intuition/intuition.h>
#include <intuition/classes.h>

#include <interfaces/drawlist.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct DrawListIFace * IDrawList = NULL;
static struct Library * __DrawListBase = NULL;
static struct DrawListIFace * __IDrawList = NULL;

/****************************************************************************/

extern struct Library * DrawListBase;

/****************************************************************************/

void drawlist_main_constructor(void)
{
    if (IDrawList != NULL)
    {
        return; /* Someone was quicker */
    }
    if (DrawListBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __DrawListBase = DrawListBase = IExec->OpenLibrary("images/drawlist.image", 0);
        if (DrawListBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "images/drawlist.image");
            abort();
        }
    }

    __IDrawList = IDrawList = (struct DrawListIFace *)IExec->GetInterface((struct Library *)DrawListBase, "main", 1, NULL);
    if (IDrawList == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "images/drawlist.image");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*drawlist_main_constructor_ptr)(void) USED = drawlist_main_constructor;

/****************************************************************************/

void drawlist_main_destructor(void)
{
    if (__IDrawList)
    {
        IExec->DropInterface ((struct Interface *)__IDrawList);
    }
    if (__DrawListBase)
    {
        IExec->CloseLibrary(__DrawListBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*drawlist_main_destructor_ptr)(void) USED = drawlist_main_destructor;

/****************************************************************************/

