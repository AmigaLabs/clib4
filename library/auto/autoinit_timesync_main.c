/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for timesync
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <libraries/timesync.h>

#include <interfaces/timesync.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct TimesyncIFace * ITimesync = NULL;
static struct Library * __TimesyncBase = NULL;
static struct TimesyncIFace * __ITimesync = NULL;

/****************************************************************************/

extern struct Library * TimesyncBase;

/****************************************************************************/

void timesync_main_constructor(void)
{
    if (ITimesync != NULL)
    {
        return; /* Someone was quicker */
    }
    if (TimesyncBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __TimesyncBase = TimesyncBase = IExec->OpenLibrary("timesync.library", 0);
        if (TimesyncBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "timesync.library");
            abort();
        }
    }

    __ITimesync = ITimesync = (struct TimesyncIFace *)IExec->GetInterface((struct Library *)TimesyncBase, "main", 1, NULL);
    if (ITimesync == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "timesync.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*timesync_main_constructor_ptr)(void) USED = timesync_main_constructor;

/****************************************************************************/

void timesync_main_destructor(void)
{
    if (__ITimesync)
    {
        IExec->DropInterface ((struct Interface *)__ITimesync);
    }
    if (__TimesyncBase)
    {
        IExec->CloseLibrary(__TimesyncBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*timesync_main_destructor_ptr)(void) USED = timesync_main_destructor;

/****************************************************************************/

