/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for gadtools
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/libraries.h>
#include <intuition/intuition.h>
#include <utility/tagitem.h>
#include <libraries/gadtools.h>

#include <interfaces/gadtools.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct GadToolsIFace * IGadTools = NULL;
static struct Library * __GadToolsBase = NULL;
static struct GadToolsIFace * __IGadTools = NULL;

/****************************************************************************/

extern struct Library * GadToolsBase;

/****************************************************************************/

void gadtools_main_constructor(void)
{
    if (IGadTools != NULL)
    {
        return; /* Someone was quicker */
    }
    if (GadToolsBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __GadToolsBase = GadToolsBase = IExec->OpenLibrary("gadtools.library", 0);
        if (GadToolsBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "gadtools.library");
            abort();
        }
    }

    __IGadTools = IGadTools = (struct GadToolsIFace *)IExec->GetInterface((struct Library *)GadToolsBase, "main", 1, NULL);
    if (IGadTools == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "gadtools.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*gadtools_main_constructor_ptr)(void) USED = gadtools_main_constructor;

/****************************************************************************/

void gadtools_main_destructor(void)
{
    if (__IGadTools)
    {
        IExec->DropInterface ((struct Interface *)__IGadTools);
    }
    if (__GadToolsBase)
    {
        IExec->CloseLibrary(__GadToolsBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*gadtools_main_destructor_ptr)(void) USED = gadtools_main_destructor;

/****************************************************************************/

