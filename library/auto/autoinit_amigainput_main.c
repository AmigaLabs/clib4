/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for amigainput
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <amigainput/amigainput.h>
#include <utility/tagitem.h>

#include <interfaces/amigainput.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct AIN_IFace * IAIN_ = NULL;
static struct Library * __AIN_Base = NULL;
static struct AIN_IFace * __IAIN_ = NULL;

/****************************************************************************/

extern struct Library * AIN_Base;

/****************************************************************************/

void amigainput_main_constructor(void)
{
    if (IAIN_ != NULL)
    {
        return; /* Someone was quicker */
    }
    if (AIN_Base == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __AIN_Base = AIN_Base = IExec->OpenLibrary("amigainput.library", 0);
        if (AIN_Base == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "amigainput.library");
            abort();
        }
    }

    __IAIN_ = IAIN_ = (struct AIN_IFace *)IExec->GetInterface((struct Library *)AIN_Base, "main", 1, NULL);
    if (IAIN_ == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "amigainput.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*amigainput_main_constructor_ptr)(void) USED = amigainput_main_constructor;

/****************************************************************************/

void amigainput_main_destructor(void)
{
    if (__IAIN_)
    {
        IExec->DropInterface ((struct Interface *)__IAIN_);
    }
    if (__AIN_Base)
    {
        IExec->CloseLibrary(__AIN_Base);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*amigainput_main_destructor_ptr)(void) USED = amigainput_main_destructor;

/****************************************************************************/

