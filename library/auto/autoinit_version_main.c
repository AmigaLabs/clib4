/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for version
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/libraries.h>
#include <utility/tagitem.h>

#include <interfaces/version.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct VersionIFace * IVersion = NULL;
static struct Library * __VersionBase = NULL;
static struct VersionIFace * __IVersion = NULL;

/****************************************************************************/

extern struct Library * VersionBase;

/****************************************************************************/

void version_main_constructor(void)
{
    if (IVersion != NULL)
    {
        return; /* Someone was quicker */
    }
    if (VersionBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __VersionBase = VersionBase = IExec->OpenLibrary("version.library", 0);
        if (VersionBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "version.library");
            abort();
        }
    }

    __IVersion = IVersion = (struct VersionIFace *)IExec->GetInterface((struct Library *)VersionBase, "main", 1, NULL);
    if (IVersion == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "version.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*version_main_constructor_ptr)(void) USED = version_main_constructor;

/****************************************************************************/

void version_main_destructor(void)
{
    if (__IVersion)
    {
        IExec->DropInterface ((struct Interface *)__IVersion);
    }
    if (__VersionBase)
    {
        IExec->CloseLibrary(__VersionBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*version_main_destructor_ptr)(void) USED = version_main_destructor;

/****************************************************************************/

