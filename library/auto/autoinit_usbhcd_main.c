/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for usbhcd
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/types.h>

#include <interfaces/usbhcd.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct USBHCDIFace * IUSBHCD = NULL;
static struct Library * __USBHCDBase = NULL;
static struct USBHCDIFace * __IUSBHCD = NULL;

/****************************************************************************/

extern struct Library * USBHCDBase;

/****************************************************************************/

void usbhcd_main_constructor(void)
{
    if (IUSBHCD != NULL)
    {
        return; /* Someone was quicker */
    }
    if (USBHCDBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __USBHCDBase = USBHCDBase = IExec->OpenLibrary("usbhcd.library", 0);
        if (USBHCDBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "usbhcd.library");
            abort();
        }
    }

    __IUSBHCD = IUSBHCD = (struct USBHCDIFace *)IExec->GetInterface((struct Library *)USBHCDBase, "main", 1, NULL);
    if (IUSBHCD == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "usbhcd.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*usbhcd_main_constructor_ptr)(void) USED = usbhcd_main_constructor;

/****************************************************************************/

void usbhcd_main_destructor(void)
{
    if (__IUSBHCD)
    {
        IExec->DropInterface ((struct Interface *)__IUSBHCD);
    }
    if (__USBHCDBase)
    {
        IExec->CloseLibrary(__USBHCDBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*usbhcd_main_destructor_ptr)(void) USED = usbhcd_main_destructor;

/****************************************************************************/

