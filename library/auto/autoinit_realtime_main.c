/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for realtime
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/types.h>
#include <libraries/realtime.h>
#include <utility/tagitem.h>

#include <interfaces/realtime.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct RealTimeIFace * IRealTime = NULL;
static struct Library * __RealTimeBase = NULL;
static struct RealTimeIFace * __IRealTime = NULL;

/****************************************************************************/

extern struct Library * RealTimeBase;

/****************************************************************************/

void realtime_main_constructor(void)
{
    if (IRealTime != NULL)
    {
        return; /* Someone was quicker */
    }
    if (RealTimeBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __RealTimeBase = RealTimeBase = IExec->OpenLibrary("realtime.library", 0);
        if (RealTimeBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "realtime.library");
            abort();
        }
    }

    __IRealTime = IRealTime = (struct RealTimeIFace *)IExec->GetInterface((struct Library *)RealTimeBase, "main", 1, NULL);
    if (IRealTime == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "realtime.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*realtime_main_constructor_ptr)(void) USED = realtime_main_constructor;

/****************************************************************************/

void realtime_main_destructor(void)
{
    if (__IRealTime)
    {
        IExec->DropInterface ((struct Interface *)__IRealTime);
    }
    if (__RealTimeBase)
    {
        IExec->CloseLibrary(__RealTimeBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*realtime_main_destructor_ptr)(void) USED = realtime_main_destructor;

/****************************************************************************/

