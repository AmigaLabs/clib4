/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for console
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/libraries.h>
#include <devices/inputevent.h>
#include <devices/keymap.h>

#include <interfaces/console.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct ConsoleIFace * IConsole = NULL;
static struct Library * __ConsoleDevice = NULL;
static struct ConsoleIFace * __IConsole = NULL;

/****************************************************************************/

extern struct Library * ConsoleDevice;

/****************************************************************************/

void console_main_constructor(void)
{
    if (IConsole != NULL)
    {
        return; /* Someone was quicker */
    }
    if (ConsoleDevice == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __ConsoleDevice = ConsoleDevice = IExec->OpenLibrary("console.device", 0);
        if (ConsoleDevice == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "console.device");
            abort();
        }
    }

    __IConsole = IConsole = (struct ConsoleIFace *)IExec->GetInterface((struct Library *)ConsoleDevice, "main", 1, NULL);
    if (IConsole == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "console.device");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*console_main_constructor_ptr)(void) USED = console_main_constructor;

/****************************************************************************/

void console_main_destructor(void)
{
    if (__IConsole)
    {
        IExec->DropInterface ((struct Interface *)__IConsole);
    }
    if (__ConsoleDevice)
    {
        IExec->CloseLibrary(__ConsoleDevice);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*console_main_destructor_ptr)(void) USED = console_main_destructor;

/****************************************************************************/

