/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for penmap
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <intuition/intuition.h>
#include <intuition/classes.h>

#include <interfaces/penmap.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <proto/intuition.h>
#include <stdlib.h>

static struct Library * __IntuitionBase = NULL;
static struct IntuitionIFace *__IIntuition = NULL;
struct PenMapIFace * IPenMap = NULL;
static struct ClassLibrary * __PenMapBase = NULL;
static struct PenMapIFace * __IPenMap = NULL;

/****************************************************************************/

extern struct ClassLibrary * PenMapBase;
extern Class * PenMapClass;

/****************************************************************************/

void penmap_main_constructor(void)
{
    if (IPenMap != NULL)
    {
        return; /* Someone was quicker */
    }
    if (PenMapBase == NULL) /* Library base is NULL, we need to open it */
    {
        if (IIntuition == NULL)
        {
            if (IntuitionBase == NULL)
            {
                __IntuitionBase = IntuitionBase = IExec->OpenLibrary("intuition.library", 0);
                if (IntuitionBase == NULL)
                {
                    BPTR StdErr = IDOS->ErrorOutput();
                    if (StdErr == ZERO)
                        StdErr = IDOS->Output();
                    IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "intuition.library");
                    abort();
                }
            }
            __IIntuition = IIntuition = (struct IntuitionIFace *)IExec->GetInterface(IntuitionBase, "main", 1, NULL);
            if (IIntuition == NULL)
            {
                BPTR StdErr = IDOS->ErrorOutput();
                if (StdErr == ZERO)
                    StdErr = IDOS->Output();
                IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "intuition.library");
                abort();
            }
        }
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __PenMapBase = PenMapBase = IIntuition->OpenClass("images/penmap.image", 0, &PenMapClass);
        if (PenMapBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "images/penmap.image");
            abort();
        }
    }

    __IPenMap = IPenMap = (struct PenMapIFace *)IExec->GetInterface((struct Library *)PenMapBase, "main", 1, NULL);
    if (IPenMap == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "images/penmap.image");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*penmap_main_constructor_ptr)(void) USED = penmap_main_constructor;

/****************************************************************************/

void penmap_main_destructor(void)
{
    if (__IPenMap)
    {
        IExec->DropInterface ((struct Interface *)__IPenMap);
    }
    if (__PenMapBase != NULL)
    {
        IIntuition->CloseClass(__PenMapBase);
    }
    if (__IIntuition != NULL)
    {
        IExec->DropInterface((struct Interface *)__IIntuition);
    }
    if (__IntuitionBase != NULL)
    {
        IExec->CloseLibrary(__IntuitionBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*penmap_main_destructor_ptr)(void) USED = penmap_main_destructor;

/****************************************************************************/

