/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for colorwheel
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/types.h>
#include <gadgets/colorwheel.h>

#include <interfaces/colorwheel.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct ColorWheelIFace * IColorWheel = NULL;
static struct Library * __ColorWheelBase = NULL;
static struct ColorWheelIFace * __IColorWheel = NULL;

/****************************************************************************/

extern struct Library * ColorWheelBase;

/****************************************************************************/

void colorwheel_main_constructor(void)
{
    if (IColorWheel != NULL)
    {
        return; /* Someone was quicker */
    }
    if (ColorWheelBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __ColorWheelBase = ColorWheelBase = IExec->OpenLibrary("gadgets/colorwheel.gadget", 0);
        if (ColorWheelBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "gadgets/colorwheel.gadget");
            abort();
        }
    }

    __IColorWheel = IColorWheel = (struct ColorWheelIFace *)IExec->GetInterface((struct Library *)ColorWheelBase, "main", 1, NULL);
    if (IColorWheel == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "gadgets/colorwheel.gadget");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*colorwheel_main_constructor_ptr)(void) USED = colorwheel_main_constructor;

/****************************************************************************/

void colorwheel_main_destructor(void)
{
    if (__IColorWheel)
    {
        IExec->DropInterface ((struct Interface *)__IColorWheel);
    }
    if (__ColorWheelBase)
    {
        IExec->CloseLibrary(__ColorWheelBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*colorwheel_main_destructor_ptr)(void) USED = colorwheel_main_destructor;

/****************************************************************************/

