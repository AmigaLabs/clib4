/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for expat
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <libraries/expat.h>

#include <interfaces/expat.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct ExpatIFace * IExpat = NULL;
static struct Library * __ExpatBase = NULL;
static struct ExpatIFace * __IExpat = NULL;

/****************************************************************************/

extern struct Library * ExpatBase;

/****************************************************************************/

void expat_main_constructor(void)
{
    if (IExpat != NULL)
    {
        return; /* Someone was quicker */
    }
    if (ExpatBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __ExpatBase = ExpatBase = IExec->OpenLibrary("expat.library", 0);
        if (ExpatBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "expat.library");
            abort();
        }
    }

    __IExpat = IExpat = (struct ExpatIFace *)IExec->GetInterface((struct Library *)ExpatBase, "main", 1, NULL);
    if (IExpat == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "expat.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*expat_main_constructor_ptr)(void) USED = expat_main_constructor;

/****************************************************************************/

void expat_main_destructor(void)
{
    if (__IExpat)
    {
        IExec->DropInterface ((struct Interface *)__IExpat);
    }
    if (__ExpatBase)
    {
        IExec->CloseLibrary(__ExpatBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*expat_main_destructor_ptr)(void) USED = expat_main_destructor;

/****************************************************************************/

