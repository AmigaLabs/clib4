/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for popcycle
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <intuition/intuition.h>
#include <intuition/classes.h>

#include <interfaces/popcycle.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct PopCycleIFace * IPopCycle = NULL;
static struct Library * __PopCycleBase = NULL;
static struct PopCycleIFace * __IPopCycle = NULL;

/****************************************************************************/

extern struct Library * PopCycleBase;

/****************************************************************************/

void popcycle_main_constructor(void)
{
    if (IPopCycle != NULL)
    {
        return; /* Someone was quicker */
    }
    if (PopCycleBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __PopCycleBase = PopCycleBase = IExec->OpenLibrary("gadgets/popcycle.gadget", 0);
        if (PopCycleBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "gadgets/popcycle.gadget");
            abort();
        }
    }

    __IPopCycle = IPopCycle = (struct PopCycleIFace *)IExec->GetInterface((struct Library *)PopCycleBase, "main", 1, NULL);
    if (IPopCycle == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "gadgets/popcycle.gadget");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*popcycle_main_constructor_ptr)(void) USED = popcycle_main_constructor;

/****************************************************************************/

void popcycle_main_destructor(void)
{
    if (__IPopCycle)
    {
        IExec->DropInterface ((struct Interface *)__IPopCycle);
    }
    if (__PopCycleBase)
    {
        IExec->CloseLibrary(__PopCycleBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*popcycle_main_destructor_ptr)(void) USED = popcycle_main_destructor;

/****************************************************************************/

