/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for icon
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <workbench/workbench.h>
#include <datatypes/pictureclass.h>

#include <interfaces/icon.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct IconIFace * IIcon = NULL;
static struct Library * __IconBase = NULL;
static struct IconIFace * __IIcon = NULL;

/****************************************************************************/

extern struct Library * IconBase;

/****************************************************************************/

void icon_main_constructor(void)
{
    if (IIcon != NULL)
    {
        return; /* Someone was quicker */
    }
    if (IconBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __IconBase = IconBase = IExec->OpenLibrary("icon.library", 0);
        if (IconBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "icon.library");
            abort();
        }
    }

    __IIcon = IIcon = (struct IconIFace *)IExec->GetInterface((struct Library *)IconBase, "main", 1, NULL);
    if (IIcon == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "icon.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*icon_main_constructor_ptr)(void) USED = icon_main_constructor;

/****************************************************************************/

void icon_main_destructor(void)
{
    if (__IIcon)
    {
        IExec->DropInterface ((struct Interface *)__IIcon);
    }
    if (__IconBase)
    {
        IExec->CloseLibrary(__IconBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*icon_main_destructor_ptr)(void) USED = icon_main_destructor;

/****************************************************************************/

