/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for picture
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/types.h>
#include <intuition/intuition.h>
#include <intuition/classes.h>

#include <interfaces/picture.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct PictureIFace * IPicture = NULL;
static struct Library * __PictureBase = NULL;
static struct PictureIFace * __IPicture = NULL;

/****************************************************************************/

extern struct Library * PictureBase;

/****************************************************************************/

void picture_main_constructor(void)
{
    if (IPicture != NULL)
    {
        return; /* Someone was quicker */
    }
    if (PictureBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __PictureBase = PictureBase = IExec->OpenLibrary("datatypes/picture.datatype", 0);
        if (PictureBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "datatypes/picture.datatype");
            abort();
        }
    }

    __IPicture = IPicture = (struct PictureIFace *)IExec->GetInterface((struct Library *)PictureBase, "main", 1, NULL);
    if (IPicture == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "datatypes/picture.datatype");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*picture_main_constructor_ptr)(void) USED = picture_main_constructor;

/****************************************************************************/

void picture_main_destructor(void)
{
    if (__IPicture)
    {
        IExec->DropInterface ((struct Interface *)__IPicture);
    }
    if (__PictureBase)
    {
        IExec->CloseLibrary(__PictureBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*picture_main_destructor_ptr)(void) USED = picture_main_destructor;

/****************************************************************************/

