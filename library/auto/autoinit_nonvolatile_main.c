/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for nonvolatile
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/types.h>
#include <exec/lists.h>
#include <libraries/nonvolatile.h>

#include <interfaces/nonvolatile.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct NVIFace * INV = NULL;
static struct Library * __NVBase = NULL;
static struct NVIFace * __INV = NULL;

/****************************************************************************/

extern struct Library * NVBase;

/****************************************************************************/

void nonvolatile_main_constructor(void)
{
    if (INV != NULL)
    {
        return; /* Someone was quicker */
    }
    if (NVBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __NVBase = NVBase = IExec->OpenLibrary("nonvolatile.library", 0);
        if (NVBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "nonvolatile.library");
            abort();
        }
    }

    __INV = INV = (struct NVIFace *)IExec->GetInterface((struct Library *)NVBase, "main", 1, NULL);
    if (INV == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "nonvolatile.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*nonvolatile_main_constructor_ptr)(void) USED = nonvolatile_main_constructor;

/****************************************************************************/

void nonvolatile_main_destructor(void)
{
    if (__INV)
    {
        IExec->DropInterface ((struct Interface *)__INV);
    }
    if (__NVBase)
    {
        IExec->CloseLibrary(__NVBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*nonvolatile_main_destructor_ptr)(void) USED = nonvolatile_main_destructor;

/****************************************************************************/

