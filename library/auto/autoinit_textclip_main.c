/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for textclip
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/types.h>

#include <interfaces/textclip.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct TextClipIFace * ITextClip = NULL;
static struct Library * __TextClipBase = NULL;
static struct TextClipIFace * __ITextClip = NULL;

/****************************************************************************/

extern struct Library * TextClipBase;

/****************************************************************************/

void textclip_main_constructor(void)
{
    if (ITextClip != NULL)
    {
        return; /* Someone was quicker */
    }
    if (TextClipBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __TextClipBase = TextClipBase = IExec->OpenLibrary("textclip.library", 0);
        if (TextClipBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "textclip.library");
            abort();
        }
    }

    __ITextClip = ITextClip = (struct TextClipIFace *)IExec->GetInterface((struct Library *)TextClipBase, "main", 1, NULL);
    if (ITextClip == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "textclip.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*textclip_main_constructor_ptr)(void) USED = textclip_main_constructor;

/****************************************************************************/

void textclip_main_destructor(void)
{
    if (__ITextClip)
    {
        IExec->DropInterface ((struct Interface *)__ITextClip);
    }
    if (__TextClipBase)
    {
        IExec->CloseLibrary(__TextClipBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*textclip_main_destructor_ptr)(void) USED = textclip_main_destructor;

/****************************************************************************/

