/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for integer
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <intuition/intuition.h>
#include <intuition/classes.h>

#include <interfaces/integer.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <proto/intuition.h>
#include <stdlib.h>

static struct Library * __IntuitionBase = NULL;
static struct IntuitionIFace *__IIntuition = NULL;
struct IntegerIFace * IInteger = NULL;
static struct ClassLibrary * __IntegerBase = NULL;
static struct IntegerIFace * __IInteger = NULL;

/****************************************************************************/

extern struct ClassLibrary * IntegerBase;
extern Class * IntegerClass;

/****************************************************************************/

void integer_main_constructor(void)
{
    if (IInteger != NULL)
    {
        return; /* Someone was quicker */
    }
    if (IntegerBase == NULL) /* Library base is NULL, we need to open it */
    {
        if (IIntuition == NULL)
        {
            if (IntuitionBase == NULL)
            {
                __IntuitionBase = IntuitionBase = IExec->OpenLibrary("intuition.library", 0);
                if (IntuitionBase == NULL)
                {
                    BPTR StdErr = IDOS->ErrorOutput();
                    if (StdErr == ZERO)
                        StdErr = IDOS->Output();
                    IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "intuition.library");
                    abort();
                }
            }
            __IIntuition = IIntuition = (struct IntuitionIFace *)IExec->GetInterface(IntuitionBase, "main", 1, NULL);
            if (IIntuition == NULL)
            {
                BPTR StdErr = IDOS->ErrorOutput();
                if (StdErr == ZERO)
                    StdErr = IDOS->Output();
                IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "intuition.library");
                abort();
            }
        }
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __IntegerBase = IntegerBase = IIntuition->OpenClass("gadgets/integer.gadget", 0, &IntegerClass);
        if (IntegerBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "gadgets/integer.gadget");
            abort();
        }
    }

    __IInteger = IInteger = (struct IntegerIFace *)IExec->GetInterface((struct Library *)IntegerBase, "main", 1, NULL);
    if (IInteger == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "gadgets/integer.gadget");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*integer_main_constructor_ptr)(void) USED = integer_main_constructor;

/****************************************************************************/

void integer_main_destructor(void)
{
    if (__IInteger)
    {
        IExec->DropInterface ((struct Interface *)__IInteger);
    }
    if (__IntegerBase != NULL)
    {
        IIntuition->CloseClass(__IntegerBase);
    }
    if (__IIntuition != NULL)
    {
        IExec->DropInterface((struct Interface *)__IIntuition);
    }
    if (__IntuitionBase != NULL)
    {
        IExec->CloseLibrary(__IntuitionBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*integer_main_destructor_ptr)(void) USED = integer_main_destructor;

/****************************************************************************/

