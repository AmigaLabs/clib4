/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for dtclass
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/types.h>
#include <intuition/classes.h>
#include <intuition/classusr.h>

#include <interfaces/dtclass.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct DTClassIFace * IDTClass = NULL;
static struct Library * __DTClassBase = NULL;
static struct DTClassIFace * __IDTClass = NULL;

/****************************************************************************/

extern struct Library * DTClassBase;

/****************************************************************************/

void dtclass_main_constructor(void)
{
    if (IDTClass != NULL)
    {
        return; /* Someone was quicker */
    }
    if (DTClassBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __DTClassBase = DTClassBase = IExec->OpenLibrary("dtclass.class", 0);
        if (DTClassBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "dtclass.class");
            abort();
        }
    }

    __IDTClass = IDTClass = (struct DTClassIFace *)IExec->GetInterface((struct Library *)DTClassBase, "main", 1, NULL);
    if (IDTClass == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "dtclass.class");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*dtclass_main_constructor_ptr)(void) USED = dtclass_main_constructor;

/****************************************************************************/

void dtclass_main_destructor(void)
{
    if (__IDTClass)
    {
        IExec->DropInterface ((struct Interface *)__IDTClass);
    }
    if (__DTClassBase)
    {
        IExec->CloseLibrary(__DTClassBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*dtclass_main_destructor_ptr)(void) USED = dtclass_main_destructor;

/****************************************************************************/

