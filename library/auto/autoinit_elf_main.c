/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for elf
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <libraries/elf.h>
#include <utility/tagitem.h>

#include <interfaces/elf.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct ElfIFace * IElf = NULL;
static struct Library * __ElfBase = NULL;
static struct ElfIFace * __IElf = NULL;

/****************************************************************************/

extern struct Library * ElfBase;

/****************************************************************************/

void elf_main_constructor(void)
{
    if (IElf != NULL)
    {
        return; /* Someone was quicker */
    }
    if (ElfBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __ElfBase = ElfBase = IExec->OpenLibrary("elf.library", 0);
        if (ElfBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "elf.library");
            abort();
        }
    }

    __IElf = IElf = (struct ElfIFace *)IExec->GetInterface((struct Library *)ElfBase, "main", 1, NULL);
    if (IElf == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "elf.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*elf_main_constructor_ptr)(void) USED = elf_main_constructor;

/****************************************************************************/

void elf_main_destructor(void)
{
    if (__IElf)
    {
        IExec->DropInterface ((struct Interface *)__IElf);
    }
    if (__ElfBase)
    {
        IExec->CloseLibrary(__ElfBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*elf_main_destructor_ptr)(void) USED = elf_main_destructor;

/****************************************************************************/

