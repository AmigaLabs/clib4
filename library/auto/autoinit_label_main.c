/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for label
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <intuition/intuition.h>
#include <intuition/classes.h>

#include <interfaces/label.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct LabelIFace * ILabel = NULL;
static struct Library * __LabelBase = NULL;
static struct LabelIFace * __ILabel = NULL;

/****************************************************************************/

extern struct Library * LabelBase;

/****************************************************************************/

void label_main_constructor(void)
{
    if (ILabel != NULL)
    {
        return; /* Someone was quicker */
    }
    if (LabelBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __LabelBase = LabelBase = IExec->OpenLibrary("images/label.image", 0);
        if (LabelBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "images/label.image");
            abort();
        }
    }

    __ILabel = ILabel = (struct LabelIFace *)IExec->GetInterface((struct Library *)LabelBase, "main", 1, NULL);
    if (ILabel == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "images/label.image");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*label_main_constructor_ptr)(void) USED = label_main_constructor;

/****************************************************************************/

void label_main_destructor(void)
{
    if (__ILabel)
    {
        IExec->DropInterface ((struct Interface *)__ILabel);
    }
    if (__LabelBase)
    {
        IExec->CloseLibrary(__LabelBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*label_main_destructor_ptr)(void) USED = label_main_destructor;

/****************************************************************************/

