/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for acpi
**
**	Copyright (C) 2015 Trevor Dickinson
**	All Rights Reserved.
*/

#include <resources/acpi.h>

#include <interfaces/acpi.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct ACPIIFace * IACPI = NULL;
static struct Library * __ACPIBase = NULL;
static struct ACPIIFace * __IACPI = NULL;

/****************************************************************************/

extern struct Library * ACPIBase;

/****************************************************************************/

void acpi_main_constructor(void)
{
    if (IACPI != NULL)
    {
        return; /* Someone was quicker */
    }
    if (ACPIBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __ACPIBase = ACPIBase = IExec->OpenLibrary("acpi.resource", 0);
        if (ACPIBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "acpi.resource");
            abort();
        }
    }

    __IACPI = IACPI = (struct ACPIIFace *)IExec->GetInterface((struct Library *)ACPIBase, "main", 1, NULL);
    if (IACPI == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "acpi.resource");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*acpi_main_constructor_ptr)(void) USED = acpi_main_constructor;

/****************************************************************************/

void acpi_main_destructor(void)
{
    if (__IACPI)
    {
        IExec->DropInterface ((struct Interface *)__IACPI);
    }
    if (__ACPIBase)
    {
        IExec->CloseLibrary(__ACPIBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*acpi_main_destructor_ptr)(void) USED = acpi_main_destructor;

/****************************************************************************/

