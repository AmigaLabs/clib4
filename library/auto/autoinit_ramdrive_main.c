/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for ramdrive
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/types.h>

#include <interfaces/ramdrive.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct RamdriveIFace * IRamdrive = NULL;
static struct Library * __RamdriveDevice = NULL;
static struct RamdriveIFace * __IRamdrive = NULL;

/****************************************************************************/

extern struct Library * RamdriveDevice;

/****************************************************************************/

void ramdrive_main_constructor(void)
{
    if (IRamdrive != NULL)
    {
        return; /* Someone was quicker */
    }
    if (RamdriveDevice == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __RamdriveDevice = RamdriveDevice = IExec->OpenLibrary("ramdrive.device", 0);
        if (RamdriveDevice == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "ramdrive.device");
            abort();
        }
    }

    __IRamdrive = IRamdrive = (struct RamdriveIFace *)IExec->GetInterface((struct Library *)RamdriveDevice, "main", 1, NULL);
    if (IRamdrive == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "ramdrive.device");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*ramdrive_main_constructor_ptr)(void) USED = ramdrive_main_constructor;

/****************************************************************************/

void ramdrive_main_destructor(void)
{
    if (__IRamdrive)
    {
        IExec->DropInterface ((struct Interface *)__IRamdrive);
    }
    if (__RamdriveDevice)
    {
        IExec->CloseLibrary(__RamdriveDevice);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*ramdrive_main_destructor_ptr)(void) USED = ramdrive_main_destructor;

/****************************************************************************/

