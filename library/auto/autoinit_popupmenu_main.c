/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for popupmenu
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <classes/popupmenu.h>

#include <interfaces/popupmenu.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <proto/intuition.h>
#include <stdlib.h>

static struct Library * __IntuitionBase = NULL;
static struct IntuitionIFace *__IIntuition = NULL;
struct PopupMenuIFace * IPopupMenu = NULL;
static struct ClassLibrary * __PopupMenuBase = NULL;
static struct PopupMenuIFace * __IPopupMenu = NULL;

/****************************************************************************/

extern struct ClassLibrary * PopupMenuBase;
extern Class * PopupMenuClass;

/****************************************************************************/

void popupmenu_main_constructor(void)
{
    if (IPopupMenu != NULL)
    {
        return; /* Someone was quicker */
    }
    if (PopupMenuBase == NULL) /* Library base is NULL, we need to open it */
    {
        if (IIntuition == NULL)
        {
            if (IntuitionBase == NULL)
            {
                __IntuitionBase = IntuitionBase = IExec->OpenLibrary("intuition.library", 0);
                if (IntuitionBase == NULL)
                {
                    BPTR StdErr = IDOS->ErrorOutput();
                    if (StdErr == ZERO)
                        StdErr = IDOS->Output();
                    IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "intuition.library");
                    abort();
                }
            }
            __IIntuition = IIntuition = (struct IntuitionIFace *)IExec->GetInterface(IntuitionBase, "main", 1, NULL);
            if (IIntuition == NULL)
            {
                BPTR StdErr = IDOS->ErrorOutput();
                if (StdErr == ZERO)
                    StdErr = IDOS->Output();
                IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "intuition.library");
                abort();
            }
        }
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __PopupMenuBase = PopupMenuBase = IIntuition->OpenClass("popupmenu.class", 0, &PopupMenuClass);
        if (PopupMenuBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "popupmenu.class");
            abort();
        }
    }

    __IPopupMenu = IPopupMenu = (struct PopupMenuIFace *)IExec->GetInterface((struct Library *)PopupMenuBase, "main", 1, NULL);
    if (IPopupMenu == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "popupmenu.class");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*popupmenu_main_constructor_ptr)(void) USED = popupmenu_main_constructor;

/****************************************************************************/

void popupmenu_main_destructor(void)
{
    if (__IPopupMenu)
    {
        IExec->DropInterface ((struct Interface *)__IPopupMenu);
    }
    if (__PopupMenuBase != NULL)
    {
        IIntuition->CloseClass(__PopupMenuBase);
    }
    if (__IIntuition != NULL)
    {
        IExec->DropInterface((struct Interface *)__IIntuition);
    }
    if (__IntuitionBase != NULL)
    {
        IExec->CloseLibrary(__IntuitionBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*popupmenu_main_destructor_ptr)(void) USED = popupmenu_main_destructor;

/****************************************************************************/

