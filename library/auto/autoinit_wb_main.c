/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for wb
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/types.h>
#include <dos/dos.h>
#include <workbench/workbench.h>
#include <intuition/intuition.h>
#include <utility/tagitem.h>

#include <interfaces/wb.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct WorkbenchIFace * IWorkbench = NULL;
static struct Library * __WorkbenchBase = NULL;
static struct WorkbenchIFace * __IWorkbench = NULL;

/****************************************************************************/

extern struct Library * WorkbenchBase;

/****************************************************************************/

void wb_main_constructor(void)
{
    if (IWorkbench != NULL)
    {
        return; /* Someone was quicker */
    }
    if (WorkbenchBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __WorkbenchBase = WorkbenchBase = IExec->OpenLibrary("workbench.library", 0);
        if (WorkbenchBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "workbench.library");
            abort();
        }
    }

    __IWorkbench = IWorkbench = (struct WorkbenchIFace *)IExec->GetInterface((struct Library *)WorkbenchBase, "main", 1, NULL);
    if (IWorkbench == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "workbench.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*wb_main_constructor_ptr)(void) USED = wb_main_constructor;

/****************************************************************************/

void wb_main_destructor(void)
{
    if (__IWorkbench)
    {
        IExec->DropInterface ((struct Interface *)__IWorkbench);
    }
    if (__WorkbenchBase)
    {
        IExec->CloseLibrary(__WorkbenchBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*wb_main_destructor_ptr)(void) USED = wb_main_destructor;

/****************************************************************************/

