/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for commodities
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/types.h>
#include <exec/nodes.h>
#include <libraries/commodities.h>
#include <devices/inputevent.h>
#include <devices/keymap.h>

#include <interfaces/commodities.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct CommoditiesIFace * ICommodities = NULL;
static struct Library * __CxBase = NULL;
static struct CommoditiesIFace * __ICommodities = NULL;

/****************************************************************************/

extern struct Library * CxBase;

/****************************************************************************/

void commodities_main_constructor(void)
{
    if (ICommodities != NULL)
    {
        return; /* Someone was quicker */
    }
    if (CxBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __CxBase = CxBase = IExec->OpenLibrary("commodities.library", 0);
        if (CxBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "commodities.library");
            abort();
        }
    }

    __ICommodities = ICommodities = (struct CommoditiesIFace *)IExec->GetInterface((struct Library *)CxBase, "main", 1, NULL);
    if (ICommodities == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "commodities.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*commodities_main_constructor_ptr)(void) USED = commodities_main_constructor;

/****************************************************************************/

void commodities_main_destructor(void)
{
    if (__ICommodities)
    {
        IExec->DropInterface ((struct Interface *)__ICommodities);
    }
    if (__CxBase)
    {
        IExec->CloseLibrary(__CxBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*commodities_main_destructor_ptr)(void) USED = commodities_main_destructor;

/****************************************************************************/

