/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for scroller
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <intuition/intuition.h>
#include <intuition/classes.h>

#include <interfaces/scroller.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <proto/intuition.h>
#include <stdlib.h>

static struct Library * __IntuitionBase = NULL;
static struct IntuitionIFace *__IIntuition = NULL;
struct ScrollerIFace * IScroller = NULL;
static struct ClassLibrary * __ScrollerBase = NULL;
static struct ScrollerIFace * __IScroller = NULL;

/****************************************************************************/

extern struct ClassLibrary * ScrollerBase;
extern Class * ScrollerClass;

/****************************************************************************/

void scroller_main_constructor(void)
{
    if (IScroller != NULL)
    {
        return; /* Someone was quicker */
    }
    if (ScrollerBase == NULL) /* Library base is NULL, we need to open it */
    {
        if (IIntuition == NULL)
        {
            if (IntuitionBase == NULL)
            {
                __IntuitionBase = IntuitionBase = IExec->OpenLibrary("intuition.library", 0);
                if (IntuitionBase == NULL)
                {
                    BPTR StdErr = IDOS->ErrorOutput();
                    if (StdErr == ZERO)
                        StdErr = IDOS->Output();
                    IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "intuition.library");
                    abort();
                }
            }
            __IIntuition = IIntuition = (struct IntuitionIFace *)IExec->GetInterface(IntuitionBase, "main", 1, NULL);
            if (IIntuition == NULL)
            {
                BPTR StdErr = IDOS->ErrorOutput();
                if (StdErr == ZERO)
                    StdErr = IDOS->Output();
                IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "intuition.library");
                abort();
            }
        }
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __ScrollerBase = ScrollerBase = IIntuition->OpenClass("gadgets/scroller.gadget", 0, &ScrollerClass);
        if (ScrollerBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "gadgets/scroller.gadget");
            abort();
        }
    }

    __IScroller = IScroller = (struct ScrollerIFace *)IExec->GetInterface((struct Library *)ScrollerBase, "main", 1, NULL);
    if (IScroller == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "gadgets/scroller.gadget");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*scroller_main_constructor_ptr)(void) USED = scroller_main_constructor;

/****************************************************************************/

void scroller_main_destructor(void)
{
    if (__IScroller)
    {
        IExec->DropInterface ((struct Interface *)__IScroller);
    }
    if (__ScrollerBase != NULL)
    {
        IIntuition->CloseClass(__ScrollerBase);
    }
    if (__IIntuition != NULL)
    {
        IExec->DropInterface((struct Interface *)__IIntuition);
    }
    if (__IntuitionBase != NULL)
    {
        IExec->CloseLibrary(__IntuitionBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*scroller_main_destructor_ptr)(void) USED = scroller_main_destructor;

/****************************************************************************/

