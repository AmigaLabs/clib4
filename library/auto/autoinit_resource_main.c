/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for resource
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <libraries/resource.h>
#include <intuition/classusr.h>
#include <intuition/intuition.h>
#include <libraries/locale.h>
#include <utility/tagitem.h>

#include <interfaces/resource.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct ResourceIFace * IResource = NULL;
static struct Library * __ResourceBase = NULL;
static struct ResourceIFace * __IResource = NULL;

/****************************************************************************/

extern struct Library * ResourceBase;

/****************************************************************************/

void resource_main_constructor(void)
{
    if (IResource != NULL)
    {
        return; /* Someone was quicker */
    }
    if (ResourceBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __ResourceBase = ResourceBase = IExec->OpenLibrary("resource.library", 0);
        if (ResourceBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "resource.library");
            abort();
        }
    }

    __IResource = IResource = (struct ResourceIFace *)IExec->GetInterface((struct Library *)ResourceBase, "main", 1, NULL);
    if (IResource == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "resource.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*resource_main_constructor_ptr)(void) USED = resource_main_constructor;

/****************************************************************************/

void resource_main_destructor(void)
{
    if (__IResource)
    {
        IExec->DropInterface ((struct Interface *)__IResource);
    }
    if (__ResourceBase)
    {
        IExec->CloseLibrary(__ResourceBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*resource_main_destructor_ptr)(void) USED = resource_main_destructor;

/****************************************************************************/

