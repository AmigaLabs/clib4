/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for input
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <exec/types.h>

#include <interfaces/input.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct InputIFace * IInput = NULL;
static struct Library * __InputBase = NULL;
static struct InputIFace * __IInput = NULL;

/****************************************************************************/

extern struct Library * InputBase;

/****************************************************************************/

void input_main_constructor(void)
{
    if (IInput != NULL)
    {
        return; /* Someone was quicker */
    }
    if (InputBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __InputBase = InputBase = IExec->OpenLibrary("input.device", 0);
        if (InputBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "input.device");
            abort();
        }
    }

    __IInput = IInput = (struct InputIFace *)IExec->GetInterface((struct Library *)InputBase, "main", 1, NULL);
    if (IInput == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "input.device");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*input_main_constructor_ptr)(void) USED = input_main_constructor;

/****************************************************************************/

void input_main_destructor(void)
{
    if (__IInput)
    {
        IExec->DropInterface ((struct Interface *)__IInput);
    }
    if (__InputBase)
    {
        IExec->CloseLibrary(__InputBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*input_main_destructor_ptr)(void) USED = input_main_destructor;

/****************************************************************************/

