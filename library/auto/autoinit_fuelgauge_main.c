/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for fuelgauge
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <intuition/intuition.h>
#include <intuition/classes.h>

#include <interfaces/fuelgauge.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct FuelGaugeIFace * IFuelGauge = NULL;
static struct Library * __FuelGaugeBase = NULL;
static struct FuelGaugeIFace * __IFuelGauge = NULL;

/****************************************************************************/

extern struct Library * FuelGaugeBase;

/****************************************************************************/

void fuelgauge_main_constructor(void)
{
    if (IFuelGauge != NULL)
    {
        return; /* Someone was quicker */
    }
    if (FuelGaugeBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __FuelGaugeBase = FuelGaugeBase = IExec->OpenLibrary("gadgets/fuelgauge.gadget", 0);
        if (FuelGaugeBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "gadgets/fuelgauge.gadget");
            abort();
        }
    }

    __IFuelGauge = IFuelGauge = (struct FuelGaugeIFace *)IExec->GetInterface((struct Library *)FuelGaugeBase, "main", 1, NULL);
    if (IFuelGauge == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "gadgets/fuelgauge.gadget");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*fuelgauge_main_constructor_ptr)(void) USED = fuelgauge_main_constructor;

/****************************************************************************/

void fuelgauge_main_destructor(void)
{
    if (__IFuelGauge)
    {
        IExec->DropInterface ((struct Interface *)__IFuelGauge);
    }
    if (__FuelGaugeBase)
    {
        IExec->CloseLibrary(__FuelGaugeBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*fuelgauge_main_destructor_ptr)(void) USED = fuelgauge_main_destructor;

/****************************************************************************/

