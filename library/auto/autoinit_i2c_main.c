/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for i2c
**
**	Copyright (C) 2019-2020 Trevor Dickinson
**	All Rights Reserved.
*/

#include <interfaces/i2c.h>

#include <interfaces/i2c.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct I2CResourceIFace * II2CRes = NULL;
static struct Library * __I2CBase = NULL;
static struct I2CResourceIFace * __II2CRes = NULL;

/****************************************************************************/

extern struct Library * I2CBase;

/****************************************************************************/

void i2c_main_constructor(void)
{
    if (II2CRes != NULL)
    {
        return; /* Someone was quicker */
    }
    if (I2CBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __I2CBase = I2CBase = IExec->OpenLibrary("i2c.resource", 0);
        if (I2CBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "i2c.resource");
            abort();
        }
    }

    __II2CRes = II2CRes = (struct I2CResourceIFace *)IExec->GetInterface((struct Library *)I2CBase, "main", 1, NULL);
    if (II2CRes == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "i2c.resource");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*i2c_main_constructor_ptr)(void) USED = i2c_main_constructor;

/****************************************************************************/

void i2c_main_destructor(void)
{
    if (__II2CRes)
    {
        IExec->DropInterface ((struct Interface *)__II2CRes);
    }
    if (__I2CBase)
    {
        IExec->CloseLibrary(__I2CBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*i2c_main_destructor_ptr)(void) USED = i2c_main_destructor;

/****************************************************************************/

