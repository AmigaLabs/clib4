/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for speedbar
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <intuition/intuition.h>
#include <intuition/classes.h>

#include <interfaces/speedbar.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct SpeedBarIFace * ISpeedBar = NULL;
static struct Library * __SpeedBarBase = NULL;
static struct SpeedBarIFace * __ISpeedBar = NULL;

/****************************************************************************/

extern struct Library * SpeedBarBase;

/****************************************************************************/

void speedbar_main_constructor(void)
{
    if (ISpeedBar != NULL)
    {
        return; /* Someone was quicker */
    }
    if (SpeedBarBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __SpeedBarBase = SpeedBarBase = IExec->OpenLibrary("gadgets/speedbar.gadget", 0);
        if (SpeedBarBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "gadgets/speedbar.gadget");
            abort();
        }
    }

    __ISpeedBar = ISpeedBar = (struct SpeedBarIFace *)IExec->GetInterface((struct Library *)SpeedBarBase, "main", 1, NULL);
    if (ISpeedBar == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "gadgets/speedbar.gadget");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*speedbar_main_constructor_ptr)(void) USED = speedbar_main_constructor;

/****************************************************************************/

void speedbar_main_destructor(void)
{
    if (__ISpeedBar)
    {
        IExec->DropInterface ((struct Interface *)__ISpeedBar);
    }
    if (__SpeedBarBase)
    {
        IExec->CloseLibrary(__SpeedBarBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*speedbar_main_destructor_ptr)(void) USED = speedbar_main_destructor;

/****************************************************************************/

