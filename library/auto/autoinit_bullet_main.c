/*
**	$Id$
**	Generated by IDLTool 53.31
**	Do not edit
**
**	AutoInit stub for bullet
**
**	Copyright (c) 2010 Hyperion Entertainment CVBA.
**	All Rights Reserved.
*/

#include <utility/tagitem.h>
#include <diskfont/glyph.h>

#include <interfaces/bullet.h>
#include <proto/exec.h>
#include <proto/dos.h>
#include <stdlib.h>

struct BulletIFace * IBullet = NULL;
static struct Library * __BulletBase = NULL;
static struct BulletIFace * __IBullet = NULL;

/****************************************************************************/

extern struct Library * BulletBase;

/****************************************************************************/

void bullet_main_constructor(void)
{
    if (IBullet != NULL)
    {
        return; /* Someone was quicker */
    }
    if (BulletBase == NULL) /* Library base is NULL, we need to open it */
    {
        /* We were called before the base constructor.
         * This means we will be called _after_ the base destructor.
         * So we cant drop the interface _after_ closing the last library base,
         * we just open the library here which ensures that.
         */
        __BulletBase = BulletBase = IExec->OpenLibrary("bullet.library", 0);
        if (BulletBase == NULL)
        {
            BPTR StdErr = IDOS->ErrorOutput();
            if (StdErr == ZERO)
                StdErr = IDOS->Output();
            IDOS->FPrintf(StdErr, "ERROR: Failed to open '%s'.\n", "bullet.library");
            abort();
        }
    }

    __IBullet = IBullet = (struct BulletIFace *)IExec->GetInterface((struct Library *)BulletBase, "main", 1, NULL);
    if (IBullet == NULL)
    {
        BPTR StdErr = IDOS->ErrorOutput();
        if (StdErr == ZERO)
            StdErr = IDOS->Output();
        IDOS->FPrintf(StdErr, "ERROR: Failed to get '%s' V%ld interface for '%s'.\n", "main", 1, "bullet.library");
        abort();
    }
}
__attribute__((section(".ctors.zzzy"))) static void
(*bullet_main_constructor_ptr)(void) USED = bullet_main_constructor;

/****************************************************************************/

void bullet_main_destructor(void)
{
    if (__IBullet)
    {
        IExec->DropInterface ((struct Interface *)__IBullet);
    }
    if (__BulletBase)
    {
        IExec->CloseLibrary(__BulletBase);
    }
}
__attribute__((section(".dtors.zzzy"))) static void
(*bullet_main_destructor_ptr)(void) USED = bullet_main_destructor;

/****************************************************************************/

